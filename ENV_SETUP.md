# Настройка переменных окружения

Документ описывает все необходимые переменные окружения для работы системы и этапы их настройки.

---

## 📋 Backend (NestJS)

### Файл: `backend/.env`

**Обязательные переменные:**

```env
# База данных PostgreSQL
DATABASE_URL="postgresql://user:password@localhost:5432/minto_db"

# Telegram Bot Token (от @BotFather)
TELEGRAM_BOT_TOKEN="1234567890:ABCdefGHIjklMNOpqrsTUVwxyz"

# JWT Secret (минимум 32 символа, рекомендуется случайная строка)
JWT_SECRET="your_super_secret_jwt_key_min_32_chars_long"

# URL Telegram Mini App (для генерации WebApp ссылок)
TMA_URL="https://your-tma-domain.com"
# Для разработки: TMA_URL="http://localhost:5173"
```

**Опциональные переменные:**

```env
# Порт приложения (по умолчанию 3000)
PORT=3000

# Окружение (development, production)
NODE_ENV=development
```

**⚠️ Важно:** 
- `OPENAI_API_KEY` **НЕ** должен быть в backend `.env` (см. раздел про TMA)
- Все переменные должны быть заполнены перед первым запуском backend

---

## 📱 TMA (Telegram Mini App)

### Файл: `tma/.env`

**Обязательные переменные:**

```env
# URL backend API
VITE_API_URL="http://localhost:3000"
# Для production: VITE_API_URL="https://your-backend-domain.com"
```

**⚠️ Важно про OpenAI API Key:**

OpenAI API ключ должен быть настроен **в TMA**, а не в backend. Это означает:

1. **Вариант 1 (рекомендуется):** Ключ хранится в TMA и передаётся в backend при запросе на проверку
   - TMA отправляет ключ в заголовке или теле запроса
   - Backend использует переданный ключ для вызова OpenAI
   - Необходимо добавить эндпоинт в backend, который принимает ключ

2. **Вариант 2:** Ключ вводится администратором через интерфейс TMA и сохраняется в БД
   - Создаётся таблица `Settings` или поле в `User` (для ADMIN)
   - Ключ хранится в зашифрованном виде
   - Backend использует ключ из БД

3. **Вариант 3 (текущая реализация требует изменения):** 
   - Сейчас `AiService` использует `OPENAI_API_KEY` из backend env
   - Нужно модифицировать, чтобы ключ передавался из TMA или хранился в БД

**Для текущей реализации (временное решение):**

Если нужно запустить систему сейчас, можно временно добавить в `backend/.env`:
```env
OPENAI_API_KEY="sk-..." # Временное решение, будет перенесено в TMA
```

---

## 🔄 Этапы настройки

### Этап 1: Первоначальная настройка (перед первым запуском)

1. **Создайте `backend/.env`** на основе `backend/env.example`:
   ```bash
   cd backend
   cp env.example .env
   ```

2. **Заполните обязательные переменные:**
   - `DATABASE_URL` — подключение к PostgreSQL
   - `TELEGRAM_BOT_TOKEN` — получите у [@BotFather](https://t.me/BotFather)
   - `JWT_SECRET` — сгенерируйте случайную строку (минимум 32 символа)
   - `TMA_URL` — URL вашего TMA (для разработки: `http://localhost:5173`)

3. **Создайте `tma/.env`:**
   ```bash
   cd ../tma
   echo 'VITE_API_URL="http://localhost:3000"' > .env
   ```

4. **Настройте базу данных:**
   ```bash
   cd ../backend
   npx prisma migrate dev
   npx prisma generate
   ```

### Этап 2: Запуск в режиме разработки

1. **Запустите PostgreSQL** (локально или используйте облачный сервис)

2. **Запустите backend:**
   ```bash
   cd backend
   npm run start:dev
   ```

3. **Запустите TMA:**
   ```bash
   cd tma
   npm run dev
   ```

4. **Проверьте работу:**
   - Backend должен запуститься на `http://localhost:3000`
   - TMA должен запуститься на `http://localhost:5173`
   - Telegram Bot должен ответить на команду `/start`

### Этап 3: Настройка OpenAI (требует доработки)

**Текущее состояние:**
- `AiService` использует `OPENAI_API_KEY` из `backend/.env`
- Это не соответствует требованию хранить ключ в TMA

**Необходимые изменения:**

1. **Вариант A: Передача ключа из TMA в backend**
   - Добавить поле для ввода ключа в TMA (для ADMIN)
   - Создать эндпоинт `POST /admin/settings/openai-key`
   - Сохранять ключ в БД (зашифрованно)
   - `AiService` получает ключ из БД

2. **Вариант B: Ключ передаётся при каждом запросе**
   - TMA передаёт ключ в заголовке `X-OpenAI-Key` при создании Submission
   - Backend использует переданный ключ
   - ⚠️ Менее безопасно, ключ виден в запросах

3. **Вариант C: Ключ хранится в TMA, TMA сам вызывает OpenAI**
   - TMA напрямую вызывает OpenAI API
   - Backend не использует OpenAI
   - ⚠️ Ключ будет виден в клиентском коде (небезопасно)

**Рекомендация:** Вариант A — ключ вводится через TMA, сохраняется в БД, используется backend.

### Этап 4: Production deployment

1. **Backend:**
   - `DATABASE_URL` — production база данных
   - `TELEGRAM_BOT_TOKEN` — тот же токен
   - `JWT_SECRET` — **новый** секретный ключ (не используйте dev ключ!)
   - `TMA_URL` — production URL TMA
   - `NODE_ENV=production`
   - `PORT` — порт сервера (обычно 3000 или через reverse proxy)

2. **TMA:**
   - `VITE_API_URL` — production URL backend

3. **Безопасность:**
   - Никогда не коммитьте `.env` файлы в git
   - Используйте секреты в CI/CD (GitHub Secrets, GitLab CI Variables)
   - Для production используйте переменные окружения сервера, а не файлы `.env`

---

## 📝 Чеклист переменных

### Backend `.env`

- [ ] `DATABASE_URL` — подключение к PostgreSQL
- [ ] `TELEGRAM_BOT_TOKEN` — токен бота от @BotFather
- [ ] `JWT_SECRET` — секретный ключ для JWT (минимум 32 символа)
- [ ] `TMA_URL` — URL Telegram Mini App
- [ ] `PORT` — порт приложения (опционально, по умолчанию 3000)
- [ ] `NODE_ENV` — окружение (опционально, по умолчанию development)
- [ ] ~~`OPENAI_API_KEY`~~ — **НЕ должен быть здесь** (будет в TMA/БД)

### TMA `.env`

- [ ] `VITE_API_URL` — URL backend API
- [ ] `VITE_OPENAI_API_KEY` — **будущая переменная** для OpenAI ключа (если выбран вариант C)

---

## 🔐 Генерация JWT_SECRET

Для генерации безопасного JWT_SECRET:

```bash
# Linux/Mac
openssl rand -base64 32

# Или через Node.js
node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
```

---

## ⚠️ Важные замечания

1. **Никогда не коммитьте `.env` файлы** — они должны быть в `.gitignore`

2. **Для разных окружений используйте разные ключи:**
   - Development и Production должны иметь разные `JWT_SECRET`
   - Разные `TELEGRAM_BOT_TOKEN` для тестового и продакшн ботов

3. **OpenAI API Key:**
   - Текущая реализация требует ключ в backend (временное решение)
   - Требуется доработка для хранения ключа в TMA/БД
   - До реализации доработки можно временно использовать `backend/.env`

4. **DATABASE_URL:**
   - Для локальной разработки: `postgresql://postgres:postgres@localhost:5432/minto_db`
   - Для облачных сервисов (Supabase, Neon): используйте строку подключения из панели управления

---

## 🚀 Быстрый старт

1. Скопируйте `backend/env.example` в `backend/.env`
2. Заполните обязательные переменные
3. Создайте `tma/.env` с `VITE_API_URL`
4. Запустите миграции: `cd backend && npx prisma migrate dev`
5. Запустите приложения

---

**Последнее обновление:** 2024

