# 🎤 Аудио-сдача заданий: Часть 2 - Обработка и транскрибация

## Обзор

**Часть 2** реализует полную обработку голосовых и видео-сообщений:
- ✅ Получение голосовых/видео-сообщений в Telegram боте
- ✅ Скачивание аудио-файлов из Telegram
- ✅ Транскрибация через OpenAI Whisper
- ✅ AI-оценка транскрипта (используя существующую логику)
- ✅ Обновление Submission с результатами
- ✅ Уведомления куратору и ученику
- ✅ Кнопка "🎤 Сдать голосовым" в TMA

---

## Backend: Изменения

### 1. **AiService** (`backend/src/ai/ai.service.ts`)

#### Добавлен метод `transcribeAudio()`

```typescript
async transcribeAudio(audioBuffer: Buffer, filename: string): Promise<string>
```

**Параметры:**
- `audioBuffer` - Buffer с аудио/видео файлом
- `filename` - Имя файла с расширением (для определения MIME-type)

**Возвращает:**
- Транскрибированный текст на русском языке

**Детали:**
- Использует OpenAI Whisper API (`whisper-1` модель)
- Язык: `ru` (для лучшей точности)
- Поддерживаемые форматы: `ogg`, `mp3`, `mp4`, `mpeg`, `mpga`, `m4a`, `wav`, `webm`
- Response format: `text` (чистый текст без метаданных)

**Обработка ошибок:**
- Логирование через Logger
- Выброс исключения с понятным сообщением

---

### 2. **TelegramService** (`backend/src/telegram/telegram.service.ts`)

#### Новые обработчики сообщений

```typescript
// Обработка голосовых сообщений
bot.on('message:voice', async (ctx) => { ... });

// Обработка видео-заметок (кружков)
bot.on('message:video_note', async (ctx) => { ... });
```

**Логика:**
1. Проверка, что сообщение является reply на инструкцию
2. Получение `file_id` из голосового/видео сообщения
3. Отправка подтверждения "⏳ Обрабатываю..."
4. Вызов `AudioSubmissionsService.processVoiceSubmission()` в фоне

**Важно:**
- Если сообщение не является reply → подсказка пользователю
- Обработка выполняется асинхронно (не блокирует обработчик)
- Используется динамический импорт через `ModuleRef` для избежания циклической зависимости

#### Новый метод `getFileUrl()`

```typescript
async getFileUrl(fileId: string): Promise<string>
```

**Параметры:**
- `fileId` - file_id из Telegram

**Возвращает:**
- URL для скачивания файла из Telegram

**Детали:**
- Вызывает `bot.api.getFile(fileId)` для получения `file_path`
- Формирует URL: `https://api.telegram.org/file/bot<TOKEN>/<file_path>`

---

### 3. **AudioSubmissionsService** (`backend/src/submissions/audio-submissions.service.ts`)

#### Добавлен метод `processVoiceSubmission()`

```typescript
async processVoiceSubmission(
  telegramId: string,
  replyToMessageId: number,
  fileId: string,
): Promise<void>
```

**Параметры:**
- `telegramId` - Telegram ID пользователя
- `replyToMessageId` - message_id инструкции (из `telegramPromptMessageId`)
- `fileId` - file_id голосового/видео сообщения

**Алгоритм:**

1. **Найти пользователя** по `telegramId`
2. **Найти Submission** по `userId` и `telegramPromptMessageId`
3. **Скачать файл** из Telegram:
   - Получить URL через `TelegramService.getFileUrl()`
   - Скачать через `fetch()`
   - Преобразовать в `Buffer`
4. **Транскрибировать** через `AiService.transcribeAudio()`
5. **Оценить через AI** (если `step.requiresAiReview`):
   - Вызвать `AiService.reviewSubmission()` с транскриптом
   - Получить `aiScore` и `aiFeedback`
6. **Обновить Submission:**
   - `answerText` = транскрипт
   - `answerFileId` = file_id
   - `aiScore` = оценка ИИ
   - `aiFeedback` = комментарий ИИ
   - `status` = `AI_REVIEWED` (если ИИ оценка) или `SENT`
7. **Уведомить ученика:**
   - Если ИИ-оценка: "✅ Аудио принято! Оценка: X/10"
   - Иначе: "✅ Аудио принято! Ожидает проверки."
8. **Уведомить кураторов:**
   - Найти всех `CURATOR`/`ADMIN` с `telegramId`
   - Отправить уведомление через `notifyCuratorAboutSubmission()`

**Обработка ошибок:**
- Все ошибки логируются
- Отправляется сообщение пользователю об ошибке
- Метод не выбрасывает исключения (graceful degradation)

---

### 4. **TelegramModule** (`backend/src/telegram/telegram.module.ts`)

- Импортирован `ModuleRef` из `@nestjs/core`
- Добавлены комментарии о циклической зависимости
- `TelegramService` использует `ModuleRef` для ленивой инжекции `AudioSubmissionsService`

---

## Frontend (TMA): Изменения

### **StepPage** (`tma/src/pages/StepPage.tsx`)

#### Новый state

```typescript
const [startingAudioSubmission, setStartingAudioSubmission] = useState(false);
```

#### Новая функция `handleStartAudioSubmission()`

```typescript
const handleStartAudioSubmission = async () => {
  // 1. Вызывает POST /audio-submissions/start
  // 2. Показывает alert с инструкцией перейти в бот
  // 3. Обновляет step после создания submission
}
```

**Сообщение пользователю:**
```
✅ Инструкция отправлена!

Перейдите в диалог с ботом и отправьте голосовое сообщение 
**ответом (реплаем)** на инструкцию бота.

⚠️ Важно: отправьте аудио именно ответом на сообщение бота, 
иначе он не сможет связать его с заданием.
```

#### Новая кнопка в UI

Условия отображения:
- `(!hasSubmission || isReturned)` - нет сдачи или возвращено
- `step.expectedAnswer === 'AUDIO' || step.expectedAnswer === 'VIDEO'`

Текст кнопки:
- `🎤 Сдать голосовым сообщением` (для AUDIO)
- `📹 Сдать видео-сообщением` (для VIDEO)

Расположение:
- Под кнопкой "Отправить на проверку"
- `marginTop: '8px'` для отступа

---

## Флоу пользователя (User Flow)

### Для LEARNER:

1. **В TMA:** Открывает шаг с `expectedAnswer = AUDIO/VIDEO`
2. **В TMA:** Нажимает "🎤 Сдать голосовым сообщением"
3. **Backend:** `POST /audio-submissions/start` создаёт/обновляет Submission
4. **Telegram Bot:** Отправляет инструкцию в личный чат ученика
5. **В Telegram:** Ученик записывает голосовое сообщение
6. **В Telegram:** Ученик отправляет его **ответом (reply)** на инструкцию бота
7. **Telegram Bot:** Получает voice/video_note, проверяет reply
8. **Backend:** `AudioSubmissionsService.processVoiceSubmission()`:
   - Скачивает файл
   - Транскрибирует через Whisper
   - Оценивает через AI
   - Обновляет Submission
9. **Telegram Bot:** Отправляет ученику результат: "✅ Аудио принято! Оценка: 7/10"
10. **Telegram Bot:** Уведомляет кураторов о новой сдаче
11. **В TMA:** Ученик обновляет страницу и видит статус "🤖 Предварительно проверено ИИ"

### Для CURATOR:

1. **Telegram Bot:** Получает уведомление о новой сдаче с AI-оценкой
2. **В TMA:** Открывает `/curator/submissions/{id}` (из Части 1)
3. **В TMA:** Видит транскрибированный текст, AI-оценку, AI-feedback
4. **В TMA:** Может послушать оригинальное аудио (через `answerFileId`, если реализовано)
5. **В TMA:** Одобряет или возвращает на доработку

---

## Технические детали

### Циклическая зависимость

**Проблема:**
- `TelegramModule` импортирует `SubmissionsModule` (через `AudioSubmissionsService`)
- `SubmissionsModule` импортирует `TelegramModule` (через `TelegramService`)

**Решение:**
- `TelegramService` использует `ModuleRef` для **ленивой инжекции**
- `AudioSubmissionsService` получается динамически в `handleVoiceMessage()`
- Импорт через `await import('../submissions/audio-submissions.service')`

```typescript
const { AudioSubmissionsService } = await import('../submissions/audio-submissions.service');
const audioSubmissionsService = this.moduleRef.get(AudioSubmissionsService, { strict: false });
```

### Формат аудио-файлов

**Telegram Voice:**
- Формат: `OGG` (Opus codec)
- Расширение для Whisper: `.ogg`
- MIME-type: `audio/ogg`

**Telegram Video Note:**
- Формат: `MP4` (H.264 + AAC)
- Расширение для Whisper: `.mp4`
- MIME-type: `video/mp4`

**Whisper API поддерживает:**
- `ogg`, `mp3`, `mp4`, `mpeg`, `mpga`, `m4a`, `wav`, `webm`

### Размер файлов

**Telegram ограничения:**
- Voice: до 1 минуты (по умолчанию), до 20 МБ
- Video Note: до 1 минуты, до 20 МБ

**OpenAI Whisper:**
- Максимальный размер: 25 МБ
- Все файлы из Telegram укладываются в лимит

---

## API Endpoints

### POST /audio-submissions/start

**Описание:** Инициирует аудио-сдачу (Часть 1, уже реализовано)

**Body:**
```json
{
  "stepId": "uuid",
  "moduleId": "uuid"
}
```

**Response:**
```json
{
  "success": true,
  "message": "Instruction sent to Telegram. Please reply with audio/video message.",
  "submissionId": "uuid",
  "telegramMessageId": 12345
}
```

---

## Зависимости

### Backend (`backend/package.json`)

```json
{
  "openai": "^4.x.x",  // Уже установлено (для reviewSubmission)
  "grammy": "^1.x.x"   // Уже установлено (для Telegram бота)
}
```

**Новых зависимостей не требуется!**

### Frontend (`tma/package.json`)

Новых зависимостей не требуется.

---

## Безопасность

1. **Проверка reply:**
   - Аудио принимается только если это reply на инструкцию бота
   - `telegramPromptMessageId` связывает submission с конкретным сообщением

2. **Проверка пользователя:**
   - Submission проверяется по `userId` + `telegramPromptMessageId`
   - Нельзя отправить аудио к чужому submission

3. **Проверка доступа:**
   - Модуль должен быть открыт (`enrollment.status = IN_PROGRESS`)
   - Нельзя пересдать одобренный шаг (`CURATOR_APPROVED`)

4. **Валидация типа шага:**
   - Аудио-сдача доступна только для `expectedAnswer = AUDIO/VIDEO`

---

## Логирование

### AiService

```
[AiService] Starting transcription for file: audio.ogg
[AiService] Transcription completed: [первые 100 символов]...
[AiService] Error transcribing audio: [error message]
```

### TelegramService

```
[TelegramService] Received voice from 123456789, reply_to: 789, file_id: ABC...
[TelegramService] Failed to get AudioSubmissionsService: [error]
```

### AudioSubmissionsService

```
[AudioSubmissionsService] Processing voice submission abc123 for user xyz456
[AudioSubmissionsService] Downloaded audio file, size: 54321 bytes
[AudioSubmissionsService] Transcription: [первые 100 символов]...
[AudioSubmissionsService] AI review completed: score 7/10
[AudioSubmissionsService] Voice submission abc123 processed successfully
[AudioSubmissionsService] Error processing voice submission: [error]
```

---

## Тестирование (Manual QA)

### Сценарий 1: Успешная аудио-сдача

1. ✅ Создать шаг с `expectedAnswer = AUDIO`, `requiresAiReview = true`
2. ✅ Открыть модуль для ученика
3. ✅ В TMA нажать "🎤 Сдать голосовым сообщением"
4. ✅ Проверить, что в Telegram пришла инструкция
5. ✅ Записать и отправить голосовое **ответом** на инструкцию
6. ✅ Проверить, что бот ответил "⏳ Обрабатываю..."
7. ✅ Дождаться обработки (~5-15 секунд)
8. ✅ Проверить, что ученик получил "✅ Аудио принято! Оценка: X/10"
9. ✅ Проверить, что куратор получил уведомление
10. ✅ В TMA обновить страницу, проверить статус "🤖 Предварительно проверено ИИ"
11. ✅ Куратор может посмотреть транскрипт и AI-фидбек в `/curator/submissions/{id}`

### Сценарий 2: Ошибка - не reply

1. ✅ Записать голосовое сообщение
2. ✅ Отправить его боту **не ответом** (просто отправить)
3. ✅ Проверить, что бот ответил подсказкой: "⚠️ Чтобы сдать аудио-задание, отправьте..."

### Сценарий 3: Ошибка - reply не на ту инструкцию

1. ✅ Отправить аудио ответом на старое сообщение бота (не инструкцию)
2. ✅ Проверить, что бот ответил: "❌ Не удалось найти задание..."

### Сценарий 4: Видео-заметка (кружок)

1. ✅ Создать шаг с `expectedAnswer = VIDEO`
2. ✅ Запустить аудио-сдачу
3. ✅ Отправить видео-кружок (video_note)
4. ✅ Проверить успешную обработку

---

## Известные ограничения

1. **Транскрипция качества:**
   - Зависит от качества аудио
   - Фоновый шум может ухудшить результат
   - Whisper лучше работает с чётким произношением

2. **Время обработки:**
   - Скачивание: 1-3 сек
   - Транскрипция Whisper: 3-10 сек (зависит от длины)
   - AI-оценка: 2-5 сек
   - **Итого:** 5-20 секунд

3. **Telegram rate limits:**
   - Скачивание файлов ограничено Telegram API
   - При массовой сдаче могут быть задержки

4. **Стоимость OpenAI:**
   - Whisper: $0.006 / минута
   - GPT-4: ~$0.03 / запрос (для AI-оценки)
   - Для курса из 10 модулей и 50 учеников: ~$30-50

---

## Будущие улучшения (Future Enhancements)

1. **Проигрывание аудио в TMA:**
   - Получить file из Telegram через bot API
   - Встроить audio player на `CuratorSubmissionPage`

2. **Повторная транскрипция:**
   - Если Whisper сделал ошибки, куратор может запросить повтор
   - Добавить кнопку "🔄 Транскрибировать заново"

3. **Промежуточные уведомления:**
   - "⏳ Транскрибация..." (через 3 сек)
   - "🤖 Оценка ИИ..." (через 10 сек)

4. **Кеширование транскриптов:**
   - Сохранять транскрипты для повторного использования
   - Не транскрибировать один файл дважды

5. **Поддержка длинных аудио:**
   - Whisper API ограничен 25 МБ
   - Для длинных файлов: разбивка на фрагменты

---

## Чеклист развёртывания

### Backend:

- [ ] Обновить `backend/src/ai/ai.service.ts`
- [ ] Обновить `backend/src/telegram/telegram.service.ts`
- [ ] Обновить `backend/src/telegram/telegram.module.ts`
- [ ] Обновить `backend/src/submissions/audio-submissions.service.ts`
- [ ] Убедиться, что `OPENAI_API_KEY` в `.env`
- [ ] `npm install` (если были добавлены зависимости)
- [ ] `npm run build`
- [ ] Перезапустить backend (`pm2 restart minto-backend`)

### Frontend (TMA):

- [ ] Обновить `tma/src/pages/StepPage.tsx`
- [ ] `npm install` (не требуется, новых зависимостей нет)
- [ ] `npm run build`
- [ ] Развернуть статику на сервере

### Тестирование:

- [ ] Проверить логи backend: `pm2 logs minto-backend`
- [ ] Проверить, что бот получает голосовые сообщения
- [ ] Проверить сценарий 1 (успешная сдача)
- [ ] Проверить сценарий 2 (ошибка - не reply)
- [ ] Проверить уведомления куратору

---

## Связанные файлы

### Часть 1 (Инициация):
- `AUDIO_SUBMISSION_FEATURE_PART1.md`
- `DEPLOY_AUDIO_SUBMISSION_PART1.sh`

### Часть 2 (Обработка):
- `AUDIO_SUBMISSION_FEATURE_PART2.md` ← **этот файл**
- `DEPLOY_AUDIO_SUBMISSION_PART2.sh`

---

## Контакты и поддержка

При возникновении проблем:
1. Проверить логи backend: `pm2 logs minto-backend --lines 100`
2. Проверить логи Telegram бота в консоли
3. Проверить, что `OPENAI_API_KEY` валиден
4. Проверить, что бот получил права на чтение сообщений в Telegram

**Автор документации:** AI Assistant (Cursor)  
**Дата:** 2025-11-29  
**Версия:** 1.0

