# Улучшение UI карточек учеников (мобильная версия)

## Проблема

На вкладке **"Ученики"** у куратора карточки участников выглядели криво:
- Огромный текст справа: "0 завершено, 1 в процессе / 0 всего, 0 на проверке"
- Текст расползался на несколько строк
- На мобильном Telegram всё съезжало
- Плохая читаемость на маленьких экранах

**Пример проблемы:**
```
┌──────────────────────────────────────┐
│ Иван Петров            0 завершено,  │
│ Менеджер               1 в процессе  │
│                        ──────────    │
│                        0 всего, 0 на │
│                        проверке      │  ← Текст разваливается
└──────────────────────────────────────┘
```

## Решение

Переверстали карточку в **компактный вертикальный формат**:

### Новая структура

```
┌─────────────────────────────────┐
│ Иван Петров          [2]        │  ← Имя + Бейдж (на проверке)
│ Менеджер                        │  ← Должность
│ ─────────────────────────────── │
│ 📖 Модулей: 1/4 (1 завершено)  │  ← Компактная статистика
│ 📝 Сдач: 3 (2 на проверке)     │
└─────────────────────────────────┘
```

### Ключевые изменения

**1. Компактный формат статистики:**

| Было | Стало |
|------|-------|
| "0 завершено, 1 в процессе" | "Модулей: 0/1 (0 завершено)" |
| "0 всего, 0 на проверке" | "Сдач: 0 (0 на проверке)" |

**2. Вертикальный layout:**
- Нет `space-between` (который растягивал текст)
- Каждая stat-item — отдельная строка
- Иконки для визуальной навигации (📖, 📝)

**3. Визуальная иерархия:**
- **Имя:** 16px, font-weight 600 (самое заметное)
- **Должность:** 13px, серый цвет (вторичный текст)
- **Статистика:** 14px, нормальный шрифт (не огромный)
- **Бейдж:** Оранжевый, в header (всегда на виду)

**4. Мобильные оптимизации:**
- `text-overflow: ellipsis` для длинных имён
- `white-space: nowrap` для должности
- Правильные gaps между элементами
- Hover/active состояния для кликабельности

## Код

### TypeScript (CuratorDashboardPage.tsx)

**Было:**
```tsx
<div className="learner-stats">
  <div className="stat-item">
    <span className="stat-label">Модули:</span>
    <span className="stat-value">
      {completedModules} завершено, {inProgressModules} в процессе
    </span>
  </div>
  <div className="stat-item">
    <span className="stat-label">Сдач:</span>
    <span className="stat-value">
      {learner.totalSubmissions} всего, {learner.pendingSubmissions} на проверке
    </span>
  </div>
</div>
```

**Стало:**
```tsx
const totalModules = learner.enrollments.length;

<div className="learner-card-header">
  <div className="learner-main-info">
    <div className="card-title">{userName}</div>
    {learner.position && (
      <div className="card-subtitle">{learner.position}</div>
    )}
  </div>
  {learner.pendingSubmissions > 0 && (
    <div className="pending-badge">
      {learner.pendingSubmissions}
    </div>
  )}
</div>

<div className="learner-stats">
  <div className="stat-item">
    <span className="stat-icon">📖</span>
    <span className="stat-text">
      Модулей: <strong>{completedModules}/{totalModules}</strong> 
      {completedModules > 0 && ` (${completedModules} завершено)`}
    </span>
  </div>
  <div className="stat-item">
    <span className="stat-icon">📝</span>
    <span className="stat-text">
      Сдач: <strong>{learner.totalSubmissions}</strong>
      {learner.pendingSubmissions > 0 && ` (${learner.pendingSubmissions} на проверке)`}
    </span>
  </div>
</div>
```

### CSS (CuratorDashboardPage.css)

**Было:**
```css
.stat-item {
  display: flex;
  justify-content: space-between; /* ← Проблема! */
  margin-bottom: 8px;
  font-size: 14px;
}

.stat-label {
  color: var(--tg-theme-hint-color, #999999);
  font-weight: 500;
}

.stat-value {
  color: var(--tg-theme-text-color, #000000);
}
```

**Стало:**
```css
.learner-card-header {
  display: flex;
  justify-content: space-between;
  align-items: flex-start;
  gap: 12px;
  margin-bottom: 12px;
}

.learner-main-info {
  flex: 1;
  min-width: 0; /* Для правильного text-overflow */
}

.card-title {
  font-size: 16px;
  font-weight: 600;
  color: var(--tg-theme-text-color, #000000);
  margin-bottom: 4px;
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.card-subtitle {
  font-size: 13px;
  color: var(--tg-theme-hint-color, #999999);
  overflow: hidden;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.learner-stats {
  display: flex;
  flex-direction: column; /* Вертикальный layout */
  gap: 8px;
  padding-top: 12px;
  border-top: 1px solid rgba(0, 0, 0, 0.1);
}

.stat-item {
  display: flex;
  align-items: center; /* Не space-between! */
  gap: 8px;
  font-size: 14px;
  line-height: 1.4;
}

.stat-icon {
  font-size: 16px;
  flex-shrink: 0;
}

.stat-text {
  color: var(--tg-theme-text-color, #000000);
  flex: 1;
}

.stat-text strong {
  font-weight: 600;
}
```

## Развёртывание

```bash
ssh root@79.132.140.13
cd /var/www/tma_education

# 1. Получить обновления
git pull

# 2. Пересобрать TMA (только frontend изменился)
cd tma
npm run build

# 3. Готово!
```

## Проверка работы

### На мобильном:

1. Откройте TMA как куратор
2. Перейдите на вкладку **"Ученики"**
3. ✅ Карточки должны быть компактными
4. ✅ Имя на одной строке (с ellipsis если длинное)
5. ✅ Должность на отдельной строке
6. ✅ Статистика читается легко:
   - "📖 Модулей: 1/4 (1 завершено)"
   - "📝 Сдач: 3 (2 на проверке)"
7. ✅ Бейдж "на проверке" справа в header
8. ✅ Нет переполнения текста
9. ✅ Карточка кликабельна (hover эффект)

### Примеры карточек:

**Ученик без активности:**
```
┌─────────────────────────────────┐
│ Мария Сидорова                  │
│ Бухгалтер                       │
│ ─────────────────────────────── │
│ 📖 Модулей: 0/4                 │
│ 📝 Сдач: 0                      │
└─────────────────────────────────┘
```

**Ученик с прогрессом:**
```
┌─────────────────────────────────┐
│ Иван Петров          [2]        │
│ Менеджер                        │
│ ─────────────────────────────── │
│ 📖 Модулей: 2/4 (2 завершено)  │
│ 📝 Сдач: 8 (2 на проверке)     │
└─────────────────────────────────┘
```

**Ученик завершил всё:**
```
┌─────────────────────────────────┐
│ Анна Иванова                    │
│ Старший менеджер                │
│ ─────────────────────────────── │
│ 📖 Модулей: 4/4 (4 завершено)  │
│ 📝 Сдач: 19                     │
└─────────────────────────────────┘
```

## Изменённые файлы

- `tma/src/pages/CuratorDashboardPage.tsx`:
  - Переверстана структура карточки
  - Добавлен `totalModules` для отображения прогресса
  - Компактный формат текста
  - Иконки для визуальной навигации

- `tma/src/pages/CuratorDashboardPage.css`:
  - Вертикальный layout для stats
  - Убран `justify-content: space-between`
  - Добавлены hover/active состояния
  - Правильный text-overflow
  - Mobile-first подход

## Преимущества

✅ **Читабельность:** Всё на своём месте, ничего не съезжает  
✅ **Компактность:** Минимум строк, максимум информации  
✅ **Мобильность:** Отлично смотрится на любом экране  
✅ **UX:** Иконки помогают быстро ориентироваться  
✅ **Доступность:** Правильные touch targets для мобильных  
✅ **Производительность:** Простая структура, быстрый рендеринг  

---

**Версия:** 1.0  
**Дата:** 2025-11-29  
**Проблема:** Карточки учеников плохо смотрелись на мобильном  
**Решение:** Компактный вертикальный layout с иконками

