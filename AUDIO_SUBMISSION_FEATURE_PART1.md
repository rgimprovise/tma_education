# Функционал аудио-сдачи заданий (Часть 1: Инициация)

## 📋 Описание

Реализация сдачи заданий голосовым сообщением через Telegram-бота с автоматической транскрибацией и оценкой ИИ.

**Текущая часть (Часть 1):** Инициация аудио-сдачи из TMA и отправка инструкций в Telegram.

**Следующая часть (Часть 2):** Обработка голосовых сообщений ботом, транскрибация через Whisper, AI-оценка.

## 🎯 Общий поток работы

### Для заданий с expectedAnswer = AUDIO или VIDEO:

1. **Ученик в TMA:** Нажимает кнопку "Сдать голосовым сообщением"
2. **TMA → Backend:** `POST /audio-submissions/start` с `stepId` и `moduleId`
3. **Backend:**
   - Проверяет права доступа (модуль открыт, шаг требует аудио/видео)
   - Создаёт/обновляет `Submission` со статусом `SENT`
   - Отправляет в Telegram инструкцию с запросом отправить аудио **ответом (reply)**
   - Сохраняет `message_id` в `Submission.telegramPromptMessageId`
4. **Ученик в Telegram:** Записывает голосовое сообщение и отправляет **ответом (reply)** на сообщение бота
5. **Бот (Часть 2):**
   - Получает голосовое сообщение
   - Находит `Submission` по `reply_to_message_id` + `userId`
   - Скачивает аудио через Telegram API
   - Транскрибирует через OpenAI Whisper
   - Прогоняет текст через AI-оценку
   - Обновляет `Submission` с результатами
   - Уведомляет куратора

## 🔧 Технические изменения (Часть 1)

### Backend

#### 1. Обновлена модель `Submission` в Prisma

**Файл:** `backend/prisma/schema.prisma`

Добавлено поле:
```prisma
telegramPromptMessageId Int? // ID сообщения бота с инструкцией для аудио-сдачи
```

**Назначение:** Связывает входящее голосовое сообщение (которое приходит как reply) с конкретной сдачей.

**Миграция:** Необходимо выполнить `npx prisma migrate dev --name add_telegram_prompt_message_id`

#### 2. Новый контроллер `AudioSubmissionsController`

**Файл:** `backend/src/submissions/audio-submissions.controller.ts`

**Эндпоинт:**
```typescript
POST /audio-submissions/start
Body: { stepId: string, moduleId: string }
```

**Защита:** `@UseGuards(JwtAuthGuard)` - только авторизованные пользователи

**Логика:**
1. Получает `userId` из JWT токена
2. Вызывает `AudioSubmissionsService.startAudioSubmission()`
3. Возвращает результат

#### 3. Новый сервис `AudioSubmissionsService`

**Файл:** `backend/src/submissions/audio-submissions.service.ts`

**Метод `startAudioSubmission()`:**

**Проверки:**
1. ✅ Пользователь существует и имеет `telegramId`
2. ✅ Шаг существует и принадлежит указанному модулю
3. ✅ Шаг требует аудио/видео (`expectedAnswer = 'AUDIO' | 'VIDEO'`)
4. ✅ Модуль открыт для пользователя (`Enrollment.status = IN_PROGRESS`)
5. ✅ Submission не одобрен куратором (нельзя пересдать одобренное)

**Действия:**
1. Отправляет инструкцию в Telegram через `TelegramService.sendMessage()`
2. Получает `message_id` отправленного сообщения
3. Создаёт или обновляет `Submission`:
   - `status = SENT`
   - `answerType = step.expectedAnswer` (AUDIO или VIDEO)
   - `telegramPromptMessageId = message_id`
   - Очищает старые данные (если пересдача)
4. Возвращает результат с `submissionId` и `telegramMessageId`

**Текст инструкции:**
```
🎤 Аудио-сдача задания

📝 Задание: [название шага]

Запишите голосовое сообщение (или видео-кружок) с ответом на это задание 
и отправьте его **ответом (реплаем) на это сообщение**.

⚠️ Важно: обязательно отправьте аудио/видео **ответом на это сообщение**, 
иначе бот не сможет связать его с заданием.
```

#### 4. Обновлён `SubmissionsModule`

**Файл:** `backend/src/submissions/submissions.module.ts`

Зарегистрированы:
- `AudioSubmissionsController` в `controllers`
- `AudioSubmissionsService` в `providers` и `exports`

### Возможные ошибки и их обработка

**400 Bad Request:**
- `User not found or has no Telegram ID` - пользователь без Telegram ID
- `Step does not belong to this module` - несоответствие stepId и moduleId
- `This step does not require audio/video submission` - шаг не требует аудио/видео
- `This step is already approved by curator` - попытка пересдать одобренное задание
- `You have already submitted this step...` - сдача уже отправлена и ждёт проверки

**403 Forbidden:**
- `Module is not available for this user` - модуль не открыт для пользователя

**404 Not Found:**
- `Step not found` - шаг не существует

## 📂 Изменённые файлы

### Backend:
1. `backend/prisma/schema.prisma` - добавлено поле `telegramPromptMessageId`
2. `backend/src/submissions/audio-submissions.controller.ts` - новый контроллер
3. `backend/src/submissions/audio-submissions.service.ts` - новый сервис
4. `backend/src/submissions/submissions.module.ts` - регистрация новых компонентов

### Документация:
1. `AUDIO_SUBMISSION_FEATURE_PART1.md` - этот файл

## 🚀 Развёртывание (Часть 1)

### Шаги:

```bash
cd /var/www/tma_education

# 1. Получить обновления
git pull

# 2. Создать миграцию для нового поля
cd backend
npx prisma migrate dev --name add_telegram_prompt_message_id

# 3. Сгенерировать Prisma Client
npx prisma generate

# 4. Пересобрать backend
npm run build

# 5. Перезапустить backend
pm2 restart minto-backend

# 6. Проверить статус
pm2 status
pm2 logs minto-backend --lines 30
```

## ✅ Проверка работы (Часть 1)

**После развёртывания Части 1 можно проверить только API:**

### Через curl:

```bash
# 1. Получить JWT токен (авторизоваться через TMA)
TOKEN="your_jwt_token_here"

# 2. Инициировать аудио-сдачу
curl -X POST https://tma.n8nrgimprovise.space/api/audio-submissions/start \
  -H "Authorization: Bearer $TOKEN" \
  -H "Content-Type: application/json" \
  -d '{"stepId": "step_id_here", "moduleId": "module_id_here"}'

# Ожидаемый ответ (200 OK):
{
  "success": true,
  "message": "Instruction sent to Telegram. Please reply with audio/video message.",
  "submissionId": "...",
  "telegramMessageId": 123456
}
```

### Проверка в Telegram:

1. После вызова API
2. Проверьте Telegram (аккаунт ученика)
3. Должно прийти сообщение с инструкцией:
   ```
   🎤 Аудио-сдача задания
   
   📝 Задание: [название]
   
   Запишите голосовое сообщение...
   ```

**Полная проверка (с отправкой аудио и транскрибацией) будет доступна после Части 2.**

## 🎯 Следующая часть (Часть 2)

В следующей части будет реализовано:

1. **Обработка голосовых сообщений в Telegram боте:**
   - Обработчик для `message.voice` и `message.video_note`
   - Проверка, что сообщение является reply
   - Поиск `Submission` по `reply_to_message_id` и `userId`

2. **Скачивание и обработка аудио:**
   - Получение `file_path` через Telegram API
   - Скачивание файла
   - Сохранение локально или в memory stream

3. **Транскрибация через OpenAI Whisper:**
   - Интеграция OpenAI Whisper API
   - Преобразование аудио в текст
   - Сохранение транскрипта в `Submission.answerText`

4. **AI-оценка транскрипта:**
   - Использование существующего `AiService`
   - Получение `aiScore` и `aiFeedback`
   - Обновление `Submission.status = AI_REVIEWED`

5. **Уведомления:**
   - Уведомление куратора о новой сдаче
   - Уведомление ученика о принятии аудио и предварительной оценке

## 📝 Примечания

### Почему важен reply (ответ на сообщение)?

- **Связь с заданием:** Бот получает тысячи сообщений от разных пользователей. Reply позволяет однозначно связать аудио с конкретным заданием.
- **Надёжность:** `reply_to_message_id` → `Submission.telegramPromptMessageId` → находим нужную сдачу
- **UX:** Пользователь видит, на какое сообщение он отвечает, и не запутается

### Почему нельзя пересдать одобренное задание?

- **Целостность данных:** Одобренная сдача уже засчитана куратором
- **Прогресс:** Может быть завершён весь модуль
- **Логика:** Если нужна пересдача → куратор должен вернуть на доработку (`CURATOR_RETURNED`)

### Почему очищаем старые данные при пересдаче?

- **Чистота:** Старый `answerText`, `answerFileId`, `aiScore` больше не актуальны
- **Новая оценка:** ИИ будет оценивать новое аудио
- **Отладка:** Легче понять, какая версия ответа сейчас активна

## 🔗 Связанные файлы

- `SPEC_Minto_TMA.md` - общая спецификация
- `ARCHITECTURE.md` - архитектура системы
- `backend/src/telegram/telegram.service.ts` - Telegram интеграция
- `backend/src/ai/ai.service.ts` - AI-оценка (будет использована в Части 2)

