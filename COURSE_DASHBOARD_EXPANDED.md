# Расширенный дашборд курса (CourseDashboardPage)

## Обзор

Детальная страница курса для кураторов, отображающая полную информацию о модуле, статистику, сдачи на проверке и список участников.

---

## Маршрут

**`/curator/courses/:moduleId`**

**Доступ:** CURATOR, ADMIN

---

## Структура страницы

### 1. Header блока

**Содержимое:**
- 🔙 Кнопка "← Назад к списку курсов"
- Название курса с иконкой (📖 обычный или 🎓 экзамен)
- Бейджи:
  - "Модуль N"
  - "Экзамен" (если `isExam = true`)
- Описание курса
- Информация о шагах: "6 шагов, 4 обязательных"

**Пример:**
```
← Назад к списку курсов

┌────────────────────────────────────────────────────┐
│ 📖 Модуль 1: Введение в пирамиду Минто  │ Модуль 1 │
│                                                    │
│ Познакомьтесь с основами принципа пирамиды Минто:  │
│ как доносить главную мысль чётко и логично...      │
│                                                    │
│ [ 📝 6 шагов, 4 обязательных ]                     │
└────────────────────────────────────────────────────┘
```

---

### 2. Блок статистики

4 карточки с ключевыми показателями:

**📝 Всего шагов**
- Количество шагов в модуле
- Подсказка: "5 обязательных"

**👥 Участников**
- Всего участников с Enrollment
- Подсказка: "2 в процессе, 3 завершили"

**📊 Прогресс**
- Процент завершивших курс
- Вычисляется: `completedLearners / totalLearners * 100%`

**📋 Сдачи**
- Всего submissions по модулю
- Подсказка: "4 на проверке"

**Источник данных:** `GET /admin/course/modules/:id/stats`

---

### 3. Блок "Действия"

2 кнопки-карточки для управления:

**✏️ Редактировать курс**
- Описание: "Изменить название и описание курса"
- Ведёт на: `/curator/course/modules/:moduleId` (редактор модуля)

**📝 Управлять шагами**
- Описание: "Добавить, изменить или удалить шаги курса"
- Ведёт на: `/curator/course/modules/:moduleId/steps` (список шагов)

---

### 4. Блок "Нуждается в проверке"

**Отображается:** Если есть submissions со статусом `SENT` или `AI_REVIEWED`

**Содержимое:**
- Список последних 5 сдач на проверке
- Каждая карточка:
  - Аватар с инициалами ученика
  - ФИО ученика
  - Шаг: "Шаг 2: Анализ ситуации"
  - Статус: "Отправлено" или "Проверено ИИ"
  - Оценка ИИ (если есть): "ИИ: 7/10"
  - Стрелка "→" при hover

**Взаимодействие:**
- Клик по карточке → `/curator/users/:userId` (карточка ученика)

**Пример:**
```
Нуждается в проверке

┌──────────────────────────────────────────────────┐
│ [И]  Иван Иванов                      │ Отправлено │ →
│      Шаг 2: Анализ ситуации                     │
└──────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────┐
│ [М]  Мария Петрова        │ Проверено ИИ │ ИИ: 8/10 │ →
│      Шаг 3: Синтез выводов                      │
└──────────────────────────────────────────────────┘
```

**Источник данных:**
```typescript
GET /admin/submissions?moduleId={moduleId}&status=SENT,AI_REVIEWED
```

---

### 5. Блок "Ученики курса"

**Отображается:** Если есть участники с Enrollment для этого модуля

**Содержимое:**
- Список всех участников курса
- Каждая карточка:
  - Аватар с инициалами
  - ФИО ученика
  - Прогресс: "Прогресс: 4/6 шагов"
  - Статус: "В процессе" или "Завершил"
  - Стрелка "→" при hover

**Взаимодействие:**
- Клик по карточке → `/curator/users/:userId` (карточка ученика)

**Пример:**
```
Ученики курса

┌──────────────────────────────────────────────────┐
│ [И]  Иван Иванов                   │ В процессе │ →
│      Прогресс: 4/6 шагов                        │
└──────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────┐
│ [М]  Мария Петрова                  │ Завершил │ →
│      Прогресс: 6/6 шагов                        │
└──────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────┐
│ [А]  Алексей Смирнов               │ В процессе │ →
│      Прогресс: 2/6 шагов                        │
└──────────────────────────────────────────────────┘
```

**Источник данных:**
```typescript
GET /admin/learners
// Фильтруется на фронте по enrollments с moduleId
```

**Вычисление прогресса:**
```typescript
completedSteps = submissions
  .filter(s => s.moduleId === moduleId && s.status === 'CURATOR_APPROVED')
  .length;
```

---

### 6. Блок "Дополнительная информация"

**Содержимое:**
- Дата создания курса
- Дата последнего обновления

---

## API Endpoints

### Используемые эндпоинты:

**1. Информация о модуле:**
```
GET /admin/course/modules/:id
Response: { id, title, description, index, isExam, createdAt, updatedAt }
```

**2. Список шагов:**
```
GET /admin/course/modules/:id/steps
Response: [{ id, title, index, isRequired, ... }]
```

**3. Статистика модуля:**
```
GET /admin/course/modules/:id/stats
Response: {
  moduleId,
  totalLearners,
  inProgressLearners,
  completedLearners,
  submissionsTotal,
  submissionsOnReview
}
```

**4. Submissions на проверке:**
```
GET /admin/submissions?moduleId={id}&status=SENT,AI_REVIEWED
Response: [{
  id,
  user: { id, firstName, lastName },
  step: { id, title, index },
  status,
  aiScore,
  createdAt
}]
```

**5. Список learners:**
```
GET /admin/learners
Response: [{
  id,
  firstName,
  lastName,
  enrollments: [{ module: { id }, status }],
  submissions: [{ module: { id }, status }]
}]
```

---

## Логика фильтрации и вычислений

### Фильтрация submissions:

```typescript
// Показываем только submissions со статусом SENT или AI_REVIEWED
const submissionsOnReview = allSubmissions
  .filter(s => s.moduleId === moduleId)
  .filter(s => s.status === 'SENT' || s.status === 'AI_REVIEWED')
  .slice(0, 5); // Последние 5
```

### Фильтрация learners:

```typescript
// Показываем только learners с enrollment на этот модуль
const courseLearners = allLearners
  .map(learner => {
    const enrollment = learner.enrollments?.find(
      e => e.module?.id === moduleId
    );
    if (!enrollment) return null;

    // Вычисляем прогресс
    const completedSteps = learner.submissions?.filter(
      s => s.module?.id === moduleId && s.status === 'CURATOR_APPROVED'
    ).length || 0;

    return {
      id: learner.id,
      firstName: learner.firstName,
      lastName: learner.lastName,
      enrollment: {
        status: enrollment.status,
        progress: completedSteps,
        totalSteps: stats.totalSteps,
      },
    };
  })
  .filter(Boolean);
```

---

## UI/UX особенности

### Аватары с инициалами

```typescript
<div className="user-avatar">
  {(user.firstName?.[0] || '?').toUpperCase()}
</div>
```

**Стиль:**
- Круглый фон с `--color-primary`
- Белый текст (`--color-text-on-primary`)
- 40px × 40px
- Первая буква имени заглавная

### Status badges

**Цвета по статусам:**

| Статус | Цвет фона | Цвет текста | Текст |
|--------|-----------|-------------|-------|
| `SENT` | `#fef5e7` | `--color-warning` | Отправлено |
| `AI_REVIEWED` | `#e3f2fd` | `--color-info` | Проверено ИИ |
| `IN_PROGRESS` | `#e3f2fd` | `--color-info` | В процессе |
| `COMPLETED` | `#e8f5e9` | `--color-success` | Завершил |

### Hover эффекты

**На карточках submissions/learners:**
- Тень увеличивается (`--shadow-md`)
- Появляется синяя рамка (`--color-primary`)
- Смещение вправо (`translateX(4px)`)
- Стрелка становится ярче и сдвигается

**На action cards:**
- Тень увеличивается
- Синяя рамка
- Смещение вверх (`translateY(-4px)`)
- Стрелка становится синей и сдвигается вправо

---

## Навигация

### Из CourseDashboardPage:

- ← Назад → `/curator/courses` (список курсов)
- ✏️ Редактировать курс → `/curator/course/modules/:moduleId`
- 📝 Управлять шагами → `/curator/course/modules/:moduleId/steps`
- Клик на submission → `/curator/users/:userId`
- Клик на learner → `/curator/users/:userId`

### В CourseDashboardPage:

- Из `/curator/courses` (клик на карточку курса)
- Из `/curator/courses/:moduleId` (прямая ссылка)

---

## Мобильная адаптация

**На узких экранах:**
- Статус badges выстраиваются вертикально
- Action cards в одну колонку
- Header элементы стекируются
- Аватары и текст сохраняют читаемость

---

## Развёртывание

```bash
# На VPS
cd /var/www/tma_education
git pull

# Пересборка TMA
cd tma
npm run build

# Backend не требует изменений
```

---

## Проверка работы

1. Откройте TMA как куратор
2. Перейдите в "📚 Курсы"
3. Кликните на любой курс
4. Проверьте:
   - ✅ Header с информацией о шагах
   - ✅ 4 карточки статистики
   - ✅ Блок "Действия" с 2 кнопками
   - ✅ Блок "Нуждается в проверке" (если есть сдачи)
   - ✅ Блок "Ученики курса" (если есть участники)
   - ✅ Клики ведут на правильные страницы
   - ✅ Hover эффекты работают

---

## Будущие улучшения

### 1. Детальная страница submission

Создать отдельный маршрут:
```
/curator/submissions/:submissionId
```

С содержимым:
- Информация о задании
- Ответ ученика
- Оценка ИИ и комментарий
- Форма проверки куратора

### 2. Фильтры и сортировка

**Для submissions:**
- Фильтр по статусу
- Сортировка по дате
- Поиск по имени ученика

**Для learners:**
- Фильтр по статусу (IN_PROGRESS, COMPLETED)
- Сортировка по прогрессу
- Поиск по имени

### 3. Bulk actions

**Для submissions:**
- Выбрать несколько сдач
- Массово одобрить/вернуть

**Для learners:**
- Выбрать несколько учеников
- Массово открыть следующий модуль

### 4. Экспорт данных

- Экспорт статистики в CSV/Excel
- Экспорт списка участников с прогрессом

---

**Версия:** 1.0  
**Дата:** 2025-11-29  
**Изменения:** Расширенный дашборд курса с блоками submissions и learners

