# Руководство по переключению аккаунтов Telegram

## 📱 Проблема кэширования в Telegram Mini Apps

Telegram Mini Apps кэширует приложения на устройстве для быстрой загрузки. При переключении между аккаунтами Telegram на одном устройстве кэш не очищается автоматически, что может привести к отображению данных предыдущего пользователя.

## ✅ Рекомендуемое решение

### Для пользователей: использование кнопки "Выйти"

При переключении между аккаунтами Telegram на одном устройстве **обязательно нажимайте кнопку "Выйти" (🚪)** перед переключением:

#### Для учеников:
1. Откройте TMA
2. Найдите кнопку 🚪 в правом верхнем углу дашборда
3. Нажмите на кнопку
4. Подтвердите выход
5. Теперь можно переключить аккаунт Telegram

#### Для кураторов:
1. Откройте TMA
2. Найдите кнопку 🚪 справа от вкладок ("Курсы", "Ученики", "Конструктор")
3. Нажмите на кнопку
4. Подтвердите выход
5. Теперь можно переключить аккаунт Telegram

### Что делать, если уже переключились и видите чужой интерфейс:

1. Нажмите кнопку 🚪 "Выйти"
2. Подтвердите выход
3. Приложение перезагрузится и покажет правильный интерфейс

## 🔧 Техническое объяснение

### Как работает авторизация:

1. При первом входе в TMA:
   - Telegram передаёт `initData` с информацией о пользователе
   - Backend проверяет подпись и создаёт/находит пользователя в БД
   - Backend возвращает JWT токен
   - TMA сохраняет токен в `localStorage`

2. При следующих запусках:
   - TMA читает токен из `localStorage`
   - Отправляет запрос `GET /users/me` для получения данных пользователя
   - Если токен валиден → пользователь авторизован
   - Если токен невалиден → очистка и переход на страницу входа

3. При выходе (кнопка 🚪):
   - Удаляется токен из `localStorage`
   - Очищается состояние авторизации
   - Приложение перезагружается

### Почему возникает проблема при смене аккаунта:

- Telegram кэширует Mini Apps на устройстве
- `localStorage` в кэше сохраняется между аккаунтами
- При смене аккаунта старый JWT токен остаётся в `localStorage`
- TMA использует этот токен и показывает данные предыдущего пользователя

### Почему не используем автоматическую проверку Telegram ID:

❌ **Неправильный подход (клиентская проверка):**
```typescript
// Проверка на клиенте ненадёжна
const currentTelegramId = window.Telegram.WebApp.initDataUnsafe.user.id;
if (savedTelegramId !== currentTelegramId) {
  // Очистка данных
}
```

**Проблемы:**
- Данные в `initDataUnsafe` не защищены от изменения
- Логика на клиенте может быть обойдена
- Не соответствует принципу "данные должны храниться в БД"

✅ **Правильный подход (серверная валидация):**
- Все проверки на backend
- JWT токен содержит `userId` из БД
- Backend проверяет, что токен действительно принадлежит текущему пользователю
- Если токен невалиден → возвращается `401 Unauthorized`
- Фронтенд получает `401` → очищает данные → перенаправляет на вход

## 🎯 Наша реализация

### Текущая схема:

1. **Авторизация:**
   - Telegram → `initData` → Backend
   - Backend проверяет подпись → создаёт JWT → возвращает токен
   - TMA сохраняет токен в `localStorage`

2. **Проверка токена:**
   - При каждом запуске TMA отправляет `GET /users/me` с токеном
   - Backend проверяет JWT и возвращает данные пользователя из БД
   - Если токен невалиден → `401` → очистка на клиенте

3. **Выход:**
   - Кнопка 🚪 → удаление токена → перезагрузка
   - Следующий вход → новая авторизация → новый токен

### Безопасность:

- ✅ JWT токен содержит `userId` из БД (не Telegram ID)
- ✅ Backend проверяет валидность токена при каждом запросе
- ✅ Токен имеет ограниченный срок действия
- ✅ Все данные хранятся в БД, а не в `localStorage`
- ✅ `localStorage` используется только для JWT токена

## 📝 Инструкция для пользователей

### При первом использовании:
1. Откройте бота в Telegram
2. Нажмите кнопку "Открыть приложение" или отправьте `/start`
3. Пройдите авторизацию (автоматически через Telegram)
4. Используйте приложение

### При переключении аккаунтов на одном устройстве:

**ВАЖНО:** Перед переключением аккаунта Telegram:

1. Откройте TMA
2. Нажмите кнопку 🚪 "Выйти"
3. Подтвердите выход
4. Закройте приложение
5. Переключите аккаунт Telegram
6. Откройте бота в новом аккаунте
7. Откройте TMA → пройдёт новая авторизация

### Если забыли нажать "Выйти":

1. Видите чужой интерфейс → нажмите кнопку 🚪
2. Подтвердите выход
3. Приложение перезагрузится с правильным интерфейсом

## 🔍 Отладка

### Проверка токена в консоли браузера:

```javascript
// Посмотреть сохранённый токен
console.log('Token:', localStorage.getItem('token'));

// Очистить токен вручную
localStorage.removeItem('token');
location.reload();

// Проверить текущего пользователя
fetch('https://tma.n8nrgimprovise.space/api/users/me', {
  headers: {
    'Authorization': 'Bearer ' + localStorage.getItem('token')
  }
}).then(r => r.json()).then(console.log);
```

### Если проблема не исчезает:

1. Полностью закрыть Telegram (убить процесс)
2. Очистить кэш приложения Telegram (в настройках системы)
3. Открыть Telegram заново
4. Открыть бота
5. Открыть TMA

## 🚀 Альтернативные решения (для будущих улучшений)

### 1. Не сохранять токен в localStorage

**Идея:** Каждый раз при открытии TMA заново авторизовываться через `initData`

**Плюсы:**
- Нет проблем с кэшем
- Всегда актуальные данные
- Автоматическая проверка пользователя

**Минусы:**
- Больше запросов к backend
- Медленнее загрузка (каждый раз новая авторизация)

### 2. Короткий срок жизни JWT токена

**Идея:** Установить `expiresIn: '15m'` для JWT токена

**Плюсы:**
- Токен автоматически станет невалидным через 15 минут
- При смене аккаунта старый токен скоро протухнет

**Минусы:**
- Пользователь должен часто переавторизовываться
- Неудобно для длительного использования

### 3. Backend проверка соответствия Telegram ID

**Идея:** При каждом `GET /users/me` backend проверяет, что Telegram ID из токена совпадает с текущим

**Плюсы:**
- Надёжная серверная валидация
- Автоматическое определение несоответствия

**Минусы:**
- Нужно передавать `initData` при каждом запросе (или использовать refresh token)
- Более сложная реализация

## ✅ Текущее решение (рекомендуемое)

**Комбинация:**
1. JWT токен с разумным сроком действия (например, 7 дней)
2. Кнопка "Выйти" для ручной очистки
3. Инструкция для пользователей
4. Backend валидация токена при каждом запросе

**Почему это работает:**
- ✅ Простота реализации
- ✅ Надёжность (серверная валидация)
- ✅ Удобство для пользователей (не нужно часто авторизовываться)
- ✅ Безопасность (все данные в БД, токен имеет срок действия)
- ✅ Контроль (кнопка "Выйти" для ручной очистки)

## 📖 Связанные документы

- `FIX_TELEGRAM_CACHE_ISSUE.md` - техническая документация (устарела, использовала клиентскую проверку)
- `README.md` - общая документация проекта
- `SPEC_Minto_TMA.md` - техническая спецификация

