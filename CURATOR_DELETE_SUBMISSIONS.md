# 🗑️ Удаление сдач учеников куратором

## Обзор

Добавлена возможность для кураторов удалять сдачи (submissions) учеников, чтобы сбросить прогресс по конкретному заданию и разрешить повторную сдачу.

---

## Проблема (до изменений)

### Что было неудобно:

1. **Нельзя сбросить прогресс:**
   - Ученик сдал задание с ошибкой
   - Куратор может только одобрить или вернуть
   - Нет способа полностью очистить сдачу

2. **Застрявшие submission:**
   - Submission создан по ошибке
   - Занимает место в базе данных
   - Мешает в списке сдач

3. **Нет гибкости:**
   - Куратор не может дать "чистый лист"
   - Ученик видит старую сдачу
   - Нужно было вручную удалять из БД

---

## Решение

### Backend: Эндпоинт для удаления

**`POST /admin/submissions/:id/delete`**

**Доступ:** Только `CURATOR` и `ADMIN` роли

**Алгоритм:**
1. Найти submission с деталями (user, step, module)
2. Удалить submission из БД
3. Если модуль был `COMPLETED` → изменить на `IN_PROGRESS`
4. Отправить уведомление ученику в Telegram
5. Вернуть сообщение об успехе

**Ответ:**
```json
{
  "message": "Сдача задания удалена. Ученик Иван Иванов может выполнить задание заново."
}
```

---

### Backend: Логика в SubmissionsService

**Метод:** `deleteSubmission(submissionId: string)`

**Шаги:**

1. **Найти submission:**
   ```typescript
   const submission = await this.prisma.submission.findUnique({
     where: { id: submissionId },
     include: { user, step, module }
   });
   ```

2. **Удалить из БД:**
   ```typescript
   await this.prisma.submission.delete({
     where: { id: submissionId }
   });
   ```

3. **Уведомить ученика:**
   ```
   🔄 Сдача задания сброшена

   Модуль 1: Введение в Минто
   Шаг 2: Первое задание

   Куратор удалил вашу сдачу. 
   Вы можете выполнить задание заново.
   ```

4. **Проверить статус модуля:**
   ```typescript
   if (enrollment.status === 'COMPLETED') {
     // Модуль был завершён, но теперь шаг удалён
     // Переводим обратно в IN_PROGRESS
     await this.prisma.enrollment.update({
       where: { id: enrollment.id },
       data: { status: 'IN_PROGRESS' }
     });
   }
   ```

---

### Frontend: UI в CuratorSubmissionPage

**Новая секция:** "⚠️ Опасная зона"

Располагается в конце страницы детальной проверки сдачи (`/curator/submissions/:id`).

**Внешний вид:**
```
┌─────────────────────────────────────────────┐
│ ⚠️ Опасная зона                             │
│                                             │
│ Удаление сдачи сбросит весь прогресс        │
│ ученика по этому заданию. Ученик сможет     │
│ выполнить задание заново.                   │
│                                             │
│ [ 🗑️ Удалить сдачу ]                        │
└─────────────────────────────────────────────┘
```

**Цвета:**
- Красная левая граница
- Красный заголовок
- Красная кнопка
- Серый текст описания

**Доступность:**
- Видна всегда (для любых статусов submission)
- Работает для `SENT`, `AI_REVIEWED`, `CURATOR_APPROVED`, `CURATOR_RETURNED`

---

### Frontend: Логика handleDelete()

**Подтверждение:**
```typescript
const confirmMessage = 
  `Удалить сдачу задания?\n\n` +
  `Ученик: ${userName}\n` +
  `Модуль: ${moduleTitle}\n` +
  `Шаг: ${stepTitle}\n\n` +
  `Ученик сможет выполнить задание заново.`;

// Используем Telegram WebApp confirm
window.Telegram.WebApp.showConfirm(confirmMessage, (confirmed) => {
  if (confirmed) {
    // Удалить
  }
});
```

**Вызов API:**
```typescript
await api.post(`/admin/submissions/${submissionId}/delete`);
```

**Успех:**
```
✅ Сдача удалена. Ученик может выполнить задание заново.
```

Автоматический возврат на предыдущую страницу (`navigate(-1)`).

---

## User Flow (пользовательский сценарий)

### Для куратора:

1. **Открывает сдачу ученика:**
   - Переходит в "Ученики" → выбирает ученика
   - Кликает на карточку submission
   - Видит детали: задание, ответ, AI-оценку

2. **Прокручивает вниз:**
   - Видит секцию "⚠️ Опасная зона"
   - Читает предупреждение

3. **Нажимает "🗑️ Удалить сдачу":**
   - Появляется confirm dialog
   - Подтверждает удаление

4. **Видит успех:**
   - Alert: "Сдача удалена"
   - Возвращается на страницу ученика
   - Submission исчез из списка

### Для ученика:

1. **Получает уведомление в Telegram:**
   ```
   🔄 Сдача задания сброшена

   Модуль 1: Введение
   Шаг 2: Задание

   Куратор удалил вашу сдачу. 
   Вы можете выполнить задание заново.
   ```

2. **Открывает TMA:**
   - Видит шаг без submission
   - Может сдать задание заново
   - "Чистый лист"

3. **Если модуль был завершён:**
   - Статус модуля: `COMPLETED` → `IN_PROGRESS`
   - Ученик может завершить модуль снова

---

## Стили (CSS)

### Опасная зона

```css
.danger-zone-card {
  background-color: rgba(244, 67, 54, 0.03);
  border-left: 3px solid #f44336;
  margin-top: 24px;
}

.danger-title {
  color: #f44336;
  font-size: 16px;
}

.danger-description {
  color: #666666;
  font-size: 14px;
  line-height: 1.5;
  margin-bottom: 16px;
}

.btn-delete {
  background-color: #f44336;
  color: #ffffff;
  border: none;
  padding: 12px 24px;
  border-radius: 8px;
  font-size: 15px;
  font-weight: 500;
  cursor: pointer;
  transition: background-color 0.2s;
  width: 100%;
}

.btn-delete:hover {
  background-color: #d32f2f;
}

.btn-delete:disabled {
  opacity: 0.6;
  cursor: not-allowed;
}
```

---

## Примеры использования

### Сценарий 1: Сброс ошибочной сдачи

**Ситуация:**
- Ученик сдал задание с неправильным ответом
- Куратор вернул на доработку
- Ученик не понял, что исправить

**Решение:**
1. Куратор удаляет сдачу
2. Ученик получает уведомление
3. Ученик связывается с куратором
4. Куратор объясняет задание
5. Ученик сдаёт заново с правильным ответом

### Сценарий 2: Технический сбой

**Ситуация:**
- Submission создан с пустым ответом (баг)
- Занимает место в БД
- Мешает в интерфейсе

**Решение:**
1. Куратор удаляет некорректную сдачу
2. База данных очищена
3. Ученик может сдать корректно

### Сценарий 3: Дать второй шанс

**Ситуация:**
- Ученик сдал хорошо, но не на максимум
- Просит разрешить попробовать ещё раз
- Куратор согласен

**Решение:**
1. Куратор удаляет текущую сдачу
2. Ученик видит "чистый лист"
3. Ученик старается лучше
4. Сдаёт заново

---

## Технические детали

### Проверка роли

```typescript
@Post(':id/delete')
@Roles(UserRole.CURATOR, UserRole.ADMIN)
async deleteSubmission(@Param('id') id: string) {
  return this.submissionsService.deleteSubmission(id);
}
```

Только пользователи с ролями `CURATOR` или `ADMIN` могут удалять сдачи.

### Cascade удаление

**НЕ используется** для submissions. Удаляем явно:

```typescript
await this.prisma.submission.delete({
  where: { id: submissionId }
});
```

**Почему не cascade:**
- Контролируемое удаление
- Можем отправить уведомления
- Можем обновить статус enrollment

### Обновление статуса модуля

```typescript
const enrollment = await this.prisma.enrollment.findFirst({
  where: { userId, moduleId }
});

if (enrollment && enrollment.status === 'COMPLETED') {
  // Модуль был завершён, но теперь шаг удалён
  await this.prisma.enrollment.update({
    where: { id: enrollment.id },
    data: { status: 'IN_PROGRESS' }
  });
}
```

**Логика:**
- Если модуль `COMPLETED` (все шаги выполнены)
- После удаления одного submission → модуль неполный
- Меняем статус на `IN_PROGRESS`

---

## Безопасность

### Защита от случайного удаления

1. **Подтверждение:**
   - Обязательный confirm dialog
   - Показывает имя ученика, модуль, шаг
   - Куратор явно соглашается

2. **Telegram уведомление:**
   - Ученик всегда узнает об удалении
   - Не может быть "тихого" удаления

3. **Ограничение ролей:**
   - Только `CURATOR` и `ADMIN`
   - Ученики не могут удалять свои сдачи

### Что НЕ удаляется

- **User** - ученик остаётся в системе
- **CourseStep** - задание остаётся
- **CourseModule** - модуль остаётся
- **Enrollment** - запись о доступе остаётся (только статус меняется)

**Удаляется только:**
- `Submission` запись
- Ответ ученика (answerText, answerFileId)
- Оценки (aiScore, curatorScore)
- Feedback (aiFeedback, curatorFeedback)

---

## Развёртывание

### Backend:

```bash
cd /var/www/tma_education
git pull

cd backend
npm run build
pm2 restart minto-backend
```

### Frontend (TMA):

```bash
cd /var/www/tma_education/tma
npm run build

# Перезагрузить Caddy
sudo systemctl reload caddy
```

### Проверка:

```bash
# Логи backend
pm2 logs minto-backend --lines 50

# Должно быть:
# [NestApplication] Nest application successfully started
```

---

## Тестирование

### Сценарий 1: Удаление обычной сдачи

1. ✅ Ученик сдаёт задание (TEXT)
2. ✅ Куратор открывает сдачу
3. ✅ Прокручивает вниз → видит "Опасная зона"
4. ✅ Нажимает "Удалить сдачу"
5. ✅ Видит confirm с деталями
6. ✅ Подтверждает → сдача удалена
7. ✅ Ученик получает уведомление в Telegram
8. ✅ Ученик открывает TMA → может сдать заново

### Сценарий 2: Удаление аудио-сдачи

1. ✅ Ученик сдаёт аудио-задание
2. ✅ Whisper транскрибирует
3. ✅ AI оценивает
4. ✅ Куратор видит транскрипт + AI-оценку
5. ✅ Куратор удаляет сдачу
6. ✅ Ученик может записать аудио заново

### Сценарий 3: Статус модуля

1. ✅ Ученик завершает все шаги модуля
2. ✅ Модуль: `status = COMPLETED`
3. ✅ Куратор удаляет одну сдачу
4. ✅ Модуль: `status = IN_PROGRESS` (автоматически)
5. ✅ Ученик может завершить модуль снова

---

## Будущие улучшения

### Опционально:

1. **Массовое удаление:**
   - Удалить все сдачи ученика по модулю
   - Кнопка "Сбросить весь модуль"

2. **История удалений:**
   - Логировать удаления в отдельную таблицу
   - Показывать куратору историю

3. **Восстановление:**
   - Soft delete вместо hard delete
   - Кнопка "Восстановить" (7 дней)

4. **Быстрое удаление:**
   - Кнопка на карточке submission в списке
   - Не нужно открывать детали

---

## Файлы

### Backend:
- `backend/src/submissions/admin.controller.ts` - эндпоинт DELETE
- `backend/src/submissions/submissions.service.ts` - deleteSubmission()

### Frontend (TMA):
- `tma/src/pages/curator/CuratorSubmissionPage.tsx` - UI + handleDelete
- `tma/src/pages/curator/CuratorSubmissionPage.css` - стили danger zone

---

## API Reference

### POST /admin/submissions/:id/delete

**Auth:** Required (JWT)  
**Roles:** `CURATOR`, `ADMIN`

**Parameters:**
- `id` (path): submission ID

**Response:**
```json
{
  "message": "Сдача задания удалена. Ученик Иван Иванов может выполнить задание заново."
}
```

**Errors:**
- `404 Not Found` - Submission не найден
- `401 Unauthorized` - Нет токена
- `403 Forbidden` - Недостаточно прав (не куратор)

---

## Преимущества

1. ✅ **Гибкость для кураторов:** Полный контроль над прогрессом учеников
2. ✅ **Исправление ошибок:** Быстрое удаление некорректных сдач
3. ✅ **Улучшение UX:** Ученик может начать заново с "чистого листа"
4. ✅ **Прозрачность:** Telegram уведомления информируют ученика
5. ✅ **Безопасность:** Подтверждение перед удалением
6. ✅ **Автоматизация:** Статус модуля обновляется автоматически

---

**Автор:** AI Assistant (Cursor)  
**Дата:** 2025-11-29  
**Версия:** 1.0

