# Исправление проблемы кэширования при смене аккаунтов Telegram

## 🐛 Проблема

При переключении между аккаунтами Telegram на одном мобильном устройстве, Telegram Mini App показывает интерфейс предыдущего пользователя (например, кураторский интерфейс для ученика).

### Причина:
- Telegram WebApp кэширует приложение на устройстве
- При смене аккаунта `localStorage` не очищается
- Старые данные авторизации (JWT токен, роль) остаются в кэше
- Приложение использует закэшированные данные вместо получения новых

## ✅ Решение

### 1. Автоматическое определение смены пользователя

**Файл:** `tma/src/contexts/AuthContext.tsx`

Добавлена проверка Telegram user ID при инициализации:

```typescript
// Проверяем, не сменился ли пользователь Telegram
const currentTelegramId = window.Telegram?.WebApp?.initDataUnsafe?.user?.id?.toString();
const savedTelegramId = localStorage.getItem('telegram_id');

// Если Telegram ID изменился - очищаем всё (смена аккаунта)
if (currentTelegramId && savedTelegramId && currentTelegramId !== savedTelegramId) {
  console.log('Telegram user changed, clearing auth data');
  localStorage.clear();
  setToken(null);
  setUser(null);
  delete api.defaults.headers.common['Authorization'];
  setIsLoading(false);
  return;
}
```

**Логика:**
1. При загрузке приложения получаем текущий Telegram user ID из `window.Telegram.WebApp.initDataUnsafe.user.id`
2. Сравниваем с сохранённым ID из `localStorage.getItem('telegram_id')`
3. Если ID не совпадают → пользователь сменился → очищаем `localStorage` и состояние авторизации
4. При успешной авторизации сохраняем Telegram ID для будущих проверок

### 2. Сохранение Telegram ID при авторизации

**Метод `login()`:**

```typescript
// Сохраняем Telegram ID для проверки при следующем запуске
const currentTelegramId = window.Telegram?.WebApp?.initDataUnsafe?.user?.id?.toString();
if (currentTelegramId) {
  localStorage.setItem('telegram_id', currentTelegramId);
}
```

### 3. Полная очистка при выходе

**Метод `logout()`:**

```typescript
const logout = () => {
  setToken(null);
  setUser(null);
  localStorage.clear(); // Очищаем всё, включая telegram_id
  delete api.defaults.headers.common['Authorization'];
};
```

### 4. Кнопка "Выйти" для ручной очистки

#### Для учеников (DashboardPage)
**Файл:** `tma/src/pages/DashboardPage.tsx`

Добавлена кнопка 🚪 в правом верхнем углу дашборда:

```typescript
const handleLogout = () => {
  if (window.Telegram?.WebApp) {
    window.Telegram.WebApp.showConfirm(
      'Вы уверены, что хотите выйти? Это очистит все данные приложения.',
      (confirmed) => {
        if (confirmed) {
          logout();
          window.location.reload();
        }
      }
    );
  }
};
```

#### Для кураторов (CuratorTabBar)
**Файл:** `tma/src/components/CuratorTabBar.tsx`

Добавлена кнопка 🚪 справа от вкладок:

```typescript
<button className="logout-button" onClick={handleLogout} title="Выйти">
  🚪
</button>
```

## 🎨 Визуальные изменения

### Кнопка выхода в DashboardPage
- Круглая кнопка с иконкой 🚪
- Расположена в правом верхнем углу
- Прозрачный фон с рамкой
- Hover-эффект

### Кнопка выхода в CuratorTabBar
- Круглая кнопка 🚪 справа от вкладок
- Фиксированная ширина (не сжимается)
- Адаптивна для мобильных устройств

## 🚀 Развёртывание

### Автоматическое
```bash
cd /var/www/tma_education
git pull
cd tma
npm run build
```

### Ручное
```bash
# 1. Получить обновления
cd /var/www/tma_education
git pull

# 2. Пересобрать TMA
cd tma
npm run build

# 3. Очистить кэш браузера (если нужно)
# На мобильном: закрыть и открыть Telegram заново
```

## ✅ Как проверить

### Автоматическое определение смены пользователя:

1. **Откройте TMA как куратор на мобильном:**
   - Telegram аккаунт куратора → откройте бота → откройте TMA
   - Убедитесь, что виден кураторский интерфейс (вкладки "Курсы", "Ученики", "Конструктор")

2. **Переключитесь на аккаунт ученика:**
   - Выйдите из Telegram или переключите аккаунт
   - Войдите в аккаунт ученика
   - Откройте того же бота → откройте TMA

3. **Проверьте:**
   - ✅ TMA автоматически определит смену пользователя
   - ✅ Очистит все данные предыдущего пользователя
   - ✅ Перенаправит на страницу входа
   - ✅ После входа покажет интерфейс ученика (без кураторских вкладок)

### Ручная проверка кнопки "Выйти":

1. **Как ученик:**
   - Откройте TMA
   - Найдите кнопку 🚪 в правом верхнем углу
   - Нажмите на неё
   - Подтвердите выход
   - **Проверьте:** приложение перезагрузилось и показывает страницу входа

2. **Как куратор:**
   - Откройте TMA
   - Найдите кнопку 🚪 справа от вкладок (рядом с "Конструктор")
   - Нажмите на неё
   - Подтвердите выход
   - **Проверьте:** приложение перезагрузилось и показывает страницу входа

### Проверка на разных устройствах:

1. **Мобильное устройство (iOS/Android):**
   - Переключайтесь между аккаунтами
   - Убедитесь, что интерфейс корректен для каждого пользователя

2. **Десктоп (Mac/Windows):**
   - Переключайтесь между аккаунтами
   - Убедитесь, что интерфейс корректен

## 🔍 Отладка

Если проблема всё ещё возникает:

1. **Проверьте консоль браузера:**
   ```javascript
   // Должно быть видно при смене пользователя:
   "Telegram user changed, clearing auth data"
   ```

2. **Проверьте localStorage:**
   ```javascript
   // В консоли браузера:
   console.log('Saved ID:', localStorage.getItem('telegram_id'));
   console.log('Current ID:', window.Telegram?.WebApp?.initDataUnsafe?.user?.id);
   ```

3. **Очистите localStorage вручную:**
   ```javascript
   // В консоли браузера:
   localStorage.clear();
   location.reload();
   ```

4. **Закройте и переоткройте Telegram полностью:**
   - На мобильном: закрыть приложение Telegram из списка задач
   - Заново открыть Telegram и попробовать открыть TMA

## 📂 Изменённые файлы

### Frontend (TMA):
1. `tma/src/contexts/AuthContext.tsx`
   - Добавлена проверка смены Telegram user ID
   - Сохранение telegram_id при авторизации
   - Полная очистка localStorage при выходе

2. `tma/src/pages/DashboardPage.tsx`
   - Добавлена кнопка "Выйти" для учеников
   - Логика выхода с подтверждением

3. `tma/src/pages/DashboardPage.css`
   - Стили для header-content
   - Стили для btn-logout

4. `tma/src/components/CuratorTabBar.tsx`
   - Добавлена кнопка "Выйти" для кураторов
   - Логика выхода с подтверждением

5. `tma/src/components/CuratorTabBar.css`
   - Стили для tabs-container
   - Стили для logout-button

### Документация:
1. `FIX_TELEGRAM_CACHE_ISSUE.md` — этот файл

## 🎯 Польза

### Для пользователей:
- ✅ **Автоматическое определение:** Не нужно вручную очищать данные при смене аккаунта
- ✅ **Корректный интерфейс:** Каждый пользователь видит свой интерфейс (ученик/куратор)
- ✅ **Безопасность:** Данные одного пользователя не доступны другому
- ✅ **Удобство:** Кнопка "Выйти" для ручной очистки при необходимости

### Для разработчиков:
- ✅ **Надёжность:** Нет проблем с кэшированием
- ✅ **Прозрачность:** Логи в консоли при смене пользователя
- ✅ **Простота:** Автоматическое решение без действий пользователя

## ⚙️ Техническая информация

### Хранение данных:
- `localStorage.setItem('token', ...)` — JWT токен
- `localStorage.setItem('telegram_id', ...)` — Telegram user ID (для проверки)

### Проверка при запуске:
1. Получение `currentTelegramId` из `window.Telegram.WebApp.initDataUnsafe.user.id`
2. Получение `savedTelegramId` из `localStorage.getItem('telegram_id')`
3. Сравнение: если не совпадают → очистка всех данных

### Очистка данных:
- `localStorage.clear()` — полная очистка всех данных
- `setToken(null)`, `setUser(null)` — очистка состояния React
- `delete api.defaults.headers.common['Authorization']` — удаление заголовка из axios

## 🔗 Связанные вопросы

- Telegram WebApp cache behavior
- localStorage persistence across sessions
- User authentication in Mini Apps
- Session management in SPA (Single Page Applications)

