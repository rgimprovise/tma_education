---
alwaysApply: true
---
# 🚨 СТРОГИЕ ПРАВИЛА РАЗРАБОТКИ ДЛЯ ЭТОГО ПРОЕКТА

## ⚠️ КРИТИЧЕСКИ ВАЖНО: ВСЕГДА ПРОВЕРЯЙ ПЕРЕД КОММИТОМ

### 1. ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ ПЕРЕД КОММИТОМ

#### Backend изменения:
- [ ] **Проверить линтер**: `cd backend && npm run lint`
- [ ] **Проверить компиляцию**: `cd backend && npm run build` (должно пройти БЕЗ ОШИБОК)
- [ ] **Если изменён `schema.prisma`**:
  - [ ] Создать миграцию: `npx prisma migrate dev --name <название>`
  - [ ] Перегенерировать Prisma Client: `npx prisma generate`
  - [ ] Проверить, что `npm run build` всё ещё работает
- [ ] **Если добавлены новые зависимости**: `npm install` и проверить `npm run build`

#### TMA (Frontend) изменения:
- [ ] **Проверить линтер**: `cd tma && npm run lint`
- [ ] **Проверить компиляцию**: `cd tma && npm run build` (должно пройти БЕЗ ОШИБОК)
- [ ] **Проверить TypeScript**: `cd tma && npx tsc --noEmit` (опционально, но желательно)

#### Если изменяются ОБА (backend + TMA):
- [ ] Проверить ОБА проекта: `npm run build` в обоих директориях
- [ ] Убедиться, что API endpoints совпадают между backend и TMA

---

## 2. ПРАВИЛА ПРИ ИЗМЕНЕНИИ КОДА

### TypeScript / Prisma типы:
- ❌ **НЕ ИСПОЛЬЗУЙ `any`** без крайней необходимости
- ✅ **ВСЕГДА проверяй типы Prisma** после изменения `schema.prisma`:
  - После изменения схемы → `npx prisma generate`
  - После генерации → `npm run build` (проверить, что нет ошибок типов)
- ✅ **Используй явные типы** для DTO, интерфейсов, возвращаемых значений
- ✅ **Проверяй импорты** - все импорты должны быть корректными

### NestJS специфичные правила:
- ✅ **Все DTO должны иметь валидаторы** (`@IsString()`, `@IsOptional()`, и т.д.)
- ✅ **Все endpoints должны быть защищены guards** (`@UseGuards(JwtAuthGuard, RolesGuard)`)
- ✅ **Все сервисы должны быть `@Injectable()`**
- ✅ **Проверяй circular dependencies** - используй `ModuleRef` для lazy loading

### React / TMA специфичные правила:
- ✅ **Все компоненты должны быть типизированы** (TypeScript)
- ✅ **Проверяй импорты** - пути должны быть корректными
- ✅ **Используй правильные хуки** (`useState`, `useEffect`, и т.д.)
- ✅ **Проверяй, что все используемые API endpoints существуют в backend**

---

## 3. ПРАВИЛА ПРИ ДЕПЛОЕ НА VPS

### ОБЯЗАТЕЛЬНЫЙ ЧЕКЛИСТ ДЕПЛОЯ:

#### Backend:
```bash
cd /var/www/tma_education/backend
git pull
npx prisma generate          # ⚠️ ВСЕГДА после изменения schema.prisma
npx prisma migrate deploy     # ⚠️ ВСЕГДА если есть новые миграции
npm run build                 # ⚠️ ВСЕГДА проверять, что компилируется
pm2 restart minto-backend
```

#### TMA:
```bash
cd /var/www/tma_education/tma
git pull
npm run build                 # ⚠️ ВСЕГДА если изменялись файлы TMA
# Файлы в dist/ обновятся автоматически
```

### ⚠️ КРИТИЧЕСКИ ВАЖНО:
- **ЕСЛИ ИЗМЕНЯЛСЯ BACKEND** → пересобрать backend
- **ЕСЛИ ИЗМЕНЯЛАСЬ TMA** → пересобрать TMA
- **ЕСЛИ ИЗМЕНЯЛИСЬ ОБА** → пересобрать ОБА
- **ЕСЛИ ИЗМЕНЯЛСЯ `schema.prisma`** → `npx prisma generate` + миграция

---

## 4. ТИПИЧНЫЕ ОШИБКИ, КОТОРЫЕ НУЖНО ИЗБЕГАТЬ

### ❌ ОШИБКА #1: Забыл пересобрать TMA
**Симптомы**: Изменения в TMA не отображаются на VPS
**Решение**: ВСЕГДА указывать в инструкции деплоя пересборку TMA, если изменялись файлы TMA

### ❌ ОШИБКА #2: Забыл `npx prisma generate` после изменения schema.prisma
**Симптомы**: `TS2353: Property 'X' does not exist in type...`
**Решение**: ВСЕГДА после изменения `schema.prisma` → `npx prisma generate` → `npm run build`

### ❌ ОШИБКА #3: Неправильные типы TypeScript
**Симптомы**: `npm run build` падает с ошибками типов
**Решение**: ВСЕГДА проверять `npm run build` локально перед коммитом

### ❌ ОШИБКА #4: Неправильные импорты
**Симптомы**: `Module not found: Error: Can't resolve...`
**Решение**: Проверять все импорты, использовать правильные пути

### ❌ ОШИБКА #5: Circular dependencies
**Симптомы**: Ошибки при запуске или неожиданное поведение
**Решение**: Использовать `ModuleRef` для lazy loading сервисов

### ❌ ОШИБКА #6: Отсутствующие валидаторы в DTO
**Симптомы**: `ValidationPipe` выбрасывает ошибки
**Решение**: ВСЕГДА добавлять `@IsString()`, `@IsOptional()`, и т.д. в DTO

### ❌ ОШИБКА #7: Несоответствие API между backend и TMA
**Симптомы**: 404 или 500 ошибки в TMA
**Решение**: Проверять, что endpoints в TMA совпадают с backend

---

## 5. ПРАВИЛА ПРИ СОЗДАНИИ НОВЫХ ФИЧ

### Шаг 1: Планирование
- [ ] Определить, какие файлы нужно изменить (backend, TMA, или оба)
- [ ] Определить, нужно ли менять `schema.prisma`
- [ ] Определить, нужны ли новые зависимости

### Шаг 2: Разработка
- [ ] Изменить код
- [ ] **ПРОВЕРИТЬ `npm run build`** в соответствующей директории
- [ ] Исправить все ошибки компиляции
- [ ] Проверить линтер

### Шаг 3: Тестирование
- [ ] Локально проверить, что всё работает
- [ ] Проверить, что нет ошибок в консоли браузера (для TMA)
- [ ] Проверить, что нет ошибок в логах (для backend)

### Шаг 4: Деплой
- [ ] Создать инструкцию деплоя (если нужно)
- [ ] Указать ВСЕ шаги (backend + TMA, если нужно)
- [ ] Указать миграции и `prisma generate`, если нужно

---

## 6. ПРАВИЛА ПРИ ОТВЕТЕ НА ЗАПРОСЫ ПОЛЬЗОВАТЕЛЯ

### ВСЕГДА:
1. ✅ Проверять, что код компилируется (`npm run build`)
2. ✅ Указывать ВСЕ шаги деплоя (backend + TMA, если нужно)
3. ✅ Указывать `npx prisma generate`, если изменялся `schema.prisma`
4. ✅ Указывать миграции, если создавались новые
5. ✅ Проверять линтер перед коммитом

### НИКОГДА:
1. ❌ Не коммитить код, который не компилируется
2. ❌ Не забывать указывать пересборку TMA
3. ❌ Не забывать указывать `npx prisma generate`
4. ❌ Не использовать `any` без необходимости
5. ❌ Не создавать circular dependencies

---

## 7. ЧЕКЛИСТ ПЕРЕД КАЖДЫМ КОММИТОМ

```bash
# Backend
cd backend
npm run lint          # Проверить линтер
npm run build         # Проверить компиляцию
# Если изменялся schema.prisma:
npx prisma generate   # Обновить Prisma Client
npm run build         # Проверить ещё раз

# TMA
cd ../tma
npm run lint          # Проверить линтер
npm run build         # Проверить компиляцию

# Если всё ОК - коммитить
git add -A
git commit -m "..."
git push
```

---

## 8. ШАБЛОН ИНСТРУКЦИИ ДЕПЛОЯ

При создании инструкции деплоя ВСЕГДА используй этот шаблон:

```markdown
## Деплой на VPS

### Backend:
```bash
cd /var/www/tma_education/backend
git pull
# ⚠️ Если изменялся schema.prisma:
npx prisma generate
npx prisma migrate deploy
npm run build
pm2 restart minto-backend
```

### TMA:
```bash
cd /var/www/tma_education/tma
git pull
npm run build
# Файлы в dist/ обновятся автоматически
```
```

**ВСЕГДА указывай оба раздела, даже если изменялся только один!**

---

## 9. МОНИТОРИНГ ОШИБОК

Если пользователь сообщает об ошибке `npm run build`:
1. ✅ Немедленно проверить код локально
2. ✅ Исправить все ошибки компиляции
3. ✅ Проверить, что `npm run build` проходит
4. ✅ Закоммитить исправления
5. ✅ Обновить инструкцию деплоя, если нужно

---

## 10. ФИНАЛЬНОЕ ПРАВИЛО

**ЕСЛИ НЕ УВЕРЕН - ПРОВЕРЬ!**

Перед каждым коммитом:
- ✅ Запусти `npm run build`
- ✅ Проверь линтер
- ✅ Проверь, что нет ошибок типов
- ✅ Проверь, что все импорты корректны

**Лучше потратить 30 секунд на проверку, чем 10 минут на исправление ошибок на VPS!**

