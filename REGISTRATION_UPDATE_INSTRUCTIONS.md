# Инструкция по обновлению: Регистрация через Telegram бота

## Что нового

Добавлена полноценная регистрация пользователей через диалог с Telegram ботом:

### Функциональность:
1. **Новые пользователи** (или с незавершённой регистрацией):
   - При `/start` бот запускает диалог регистрации
   - Последовательно спрашивает: имя → фамилию → должность
   - После завершения устанавливает `profileCompleted = true`
   - Отправляет кнопку для открытия TMA

2. **Существующие пользователи** (с завершённой регистрацией):
   - При `/start` сразу получают приветствие и кнопку TMA
   - Диалог регистрации не запускается

### Изменения в БД:
- Добавлено поле `profileCompleted` (Boolean, default: false) в модель `User`

---

## Команды для обновления на VPS

### 1. Подключитесь к VPS:
```bash
ssh root@79.132.140.13
```

### 2. Перейдите в директорию проекта:
```bash
cd /var/www/tma_education
```

### 3. Получите последние изменения:
```bash
git pull
```

### 4. Перейдите в backend и примените миграцию:
```bash
cd backend
npx prisma migrate deploy
```

Эта команда применит миграцию `20251129003000_add_profile_completed_to_user`, которая добавит поле `profileCompleted` в таблицу `User`.

### 5. (Опционально) Обновите существующих пользователей:
Если хотите, чтобы существующие пользователи имели `profileCompleted = true` (не проходили регистрацию заново), выполните:

```bash
npx prisma db seed
```

Или выполните SQL напрямую:
```bash
psql -U minto_user -d minto_db -c "UPDATE \"User\" SET \"profileCompleted\" = true WHERE \"firstName\" IS NOT NULL AND \"lastName\" IS NOT NULL;"
```

### 6. Пересоберите backend:
```bash
npm run build
```

### 7. Перезапустите backend через PM2:
```bash
pm2 restart minto-backend
```

### 8. Проверьте логи:
```bash
pm2 logs minto-backend --lines 30
```

Должны увидеть:
```
[NestApplication] Nest application successfully started
🚀 Application is running on: http://localhost:3002
🤖 Telegram Bot started: @MintoEducationBot
```

---

## Проверка работы

### 1. В Telegram:
Найдите бота `@MintoEducationBot` и отправьте `/start`.

### Для нового пользователя:
1. Бот спросит: "Напишите ваше имя"
2. Вы отправляете имя → бот спросит фамилию
3. Вы отправляете фамилию → бот спросит должность
4. Вы отправляете должность → бот пишет "Регистрация завершена" и отправляет WebApp кнопку

### Для существующего пользователя:
1. Бот сразу отправляет приветствие и WebApp кнопку (без диалога)

### 2. Проверка в БД:
```bash
psql -U minto_user -d minto_db -c "SELECT \"telegramId\", \"firstName\", \"lastName\", \"position\", \"profileCompleted\" FROM \"User\" ORDER BY \"createdAt\" DESC LIMIT 5;"
```

Поле `profileCompleted` должно быть `true` для пользователей, завершивших регистрацию.

---

## Откат (если что-то пошло не так)

### 1. Откатить миграцию:
```bash
cd /var/www/tma_education/backend
npx prisma migrate resolve --rolled-back 20251129003000_add_profile_completed_to_user
```

### 2. Вернуться к предыдущей версии кода:
```bash
cd /var/www/tma_education
git reset --hard 2380fe7  # Предыдущий коммит
cd backend
npm run build
pm2 restart minto-backend
```

---

## Техническая информация

### Изменённые файлы:
- `backend/prisma/schema.prisma` — добавлено поле `profileCompleted`
- `backend/prisma/migrations/20251129003000_add_profile_completed_to_user/migration.sql` — SQL миграция
- `backend/src/telegram/telegram.service.ts` — реализация диалога регистрации
- `backend/src/users/users.service.ts` — поддержка нового поля
- `backend/prisma/seed.ts` — обновление seed-скрипта

### Архитектура регистрации:
- **FSM (Finite State Machine)**: Состояния хранятся в `Map<telegramId, RegistrationData>`
- **Состояния**: `WAITING_FIRST_NAME`, `WAITING_LAST_NAME`, `WAITING_POSITION`
- **Обработчики**:
  - `bot.command('start')` — точка входа
  - `bot.on('message:text')` — обработка ответов в диалоге

### Безопасность:
- Состояния регистрации хранятся в памяти (для production лучше использовать Redis)
- При перезапуске backend незавершённые регистрации сбрасываются (пользователи могут начать заново с `/start`)

---

## Поддержка

Если возникли проблемы:
1. Проверьте логи: `pm2 logs minto-backend`
2. Проверьте статус: `pm2 status`
3. Проверьте БД: `psql -U minto_user -d minto_db`
4. Проверьте, что миграция применена: `npx prisma migrate status`

Если бот не отвечает:
```bash
# Проверить, что bot запущен
pm2 logs minto-backend | grep "Telegram Bot"

# Должна быть строка:
# 🤖 Telegram Bot started: @MintoEducationBot
```

