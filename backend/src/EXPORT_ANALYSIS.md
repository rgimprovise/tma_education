# Анализ существующих возможностей экспорта/выгрузки данных

**Дата анализа:** 2025-11-30  
**Статус:** Только анализ, без реализации

---

## 📊 Существующие отчёты и экспорты

### 1. HTML-отчёт по курсу ✅

**Эндпоинт:**
- `GET /admin/courses/:courseId/report/html` - возвращает HTML-документ
- `POST /admin/courses/:courseId/report/send-telegram` - отправляет HTML-файл в Telegram

**Формат:** HTML (встроенный CSS, без внешних зависимостей)

**Что включает:**
- Информация о курсе (название, описание, количество модулей/шагов)
- Общие KPI: totalLearners, startedLearners, completedLearners, avgCompletionPercent, avgCompletionTime, totalSubmissions
- Таблица по модулям: индекс, название, шаги, прогресс, время прохождения, количество сдач
- Проблемные зоны: модули и шаги с низким completionRate, высоким returnsPercent, низким avgScore
- Подробная таблица по шагам: индекс, название, тип, обязательность, % завершения, сдачи, оценки ИИ/куратора, % возвратов
- Разрез по должностям: position, количество участников, средний % завершения, средний балл, среднее возвратов

**Используемые сущности:**
- `Course`, `CourseModule`, `CourseStep`
- `Enrollment` (status, unlockedAt, completedAt)
- `Submission` (status, aiScore, curatorScore, createdAt, updatedAt)
- `User` (position, role)

**Файлы:**
- `backend/src/course/course-report.service.ts` - бизнес-логика построения отчёта
- `backend/src/course/course-report-html.builder.ts` - генератор HTML
- `backend/src/course/dto/course-report.dto.ts` - TypeScript интерфейсы

---

### 2. JSON API для просмотра данных ✅

#### 2.1. Список участников с прогрессом

**Эндпоинт:** `GET /admin/learners`

**Формат:** JSON

**Что возвращает:**
- Список всех LEARNER пользователей
- Для каждого: enrollments (модули, статусы, даты), totalSubmissions, pendingSubmissions

**Используемые поля:**
- `User`: id, telegramId, firstName, lastName, position, role, createdAt
- `Enrollment`: id, status, unlockedAt, completedAt, module (id, index, title)
- `Submission`: status (для подсчёта pending)

**Файлы:**
- `backend/src/users/users.service.ts` → `getLearnersWithProgress()`
- `backend/src/users/admin.controller.ts` → `GET /admin/learners`

---

#### 2.2. Детальный прогресс участника

**Эндпоинт:** `GET /admin/learners/:id`

**Формат:** JSON

**Что возвращает:**
- Полная информация о пользователе
- Все enrollments с информацией о том, кто открыл модуль
- Последние 10 сдач (recentSubmissions)
- Статистика: totalSubmissions, approvedSubmissions, pendingSubmissions, returnedSubmissions

**Используемые поля:**
- `User`: все поля
- `Enrollment`: все поля + unlockedBy (куратор)
- `Submission`: id, status, step (id, title, index), module (id, index, title), createdAt

**Файлы:**
- `backend/src/users/users.service.ts` → `getLearnerDetail()`
- `backend/src/users/admin.controller.ts` → `GET /admin/learners/:id`

---

#### 2.3. Список сдач с фильтрами

**Эндпоинт:** `GET /admin/submissions?moduleId=...&status=...`

**Формат:** JSON

**Что возвращает:**
- Список всех сдач (или отфильтрованных по moduleId/status)
- Для каждой сдачи: полная информация включая step, module, user

**Используемые поля:**
- `Submission`: все поля (answerText, answerFileId, answerType, aiScore, aiFeedback, curatorScore, curatorFeedback, status, createdAt, updatedAt)
- `CourseStep`: id, title, index
- `CourseModule`: id, index, title
- `User`: id, firstName, lastName

**Файлы:**
- `backend/src/submissions/submissions.service.ts` → `findAllWithFilters()`
- `backend/src/submissions/admin.controller.ts` → `GET /admin/submissions`

---

#### 2.4. Детали конкретной сдачи

**Эндпоинт:** `GET /admin/submissions/:id`

**Формат:** JSON

**Что возвращает:**
- Полная информация о сдаче со всеми связанными данными

**Файлы:**
- `backend/src/submissions/submissions.service.ts` → `findById()`
- `backend/src/submissions/admin.controller.ts` → `GET /admin/submissions/:id`

---

#### 2.5. Статистика по модулю

**Эндпоинт:** `GET /admin/course/modules/:id/stats`

**Формат:** JSON

**Что возвращает:**
- totalLearners, inProgressLearners, completedLearners
- submissionsTotal, submissionsOnReview

**Файлы:**
- `backend/src/course/course-admin.service.ts` → `getModuleStats()`
- `backend/src/course/course-builder.controller.ts` → `GET /admin/course/modules/:id/stats`

---

### 3. Бинарные файлы (аудио) ✅

**Эндпоинт:** `GET /audio-submissions/play/:fileId`

**Формат:** StreamableFile (бинарный поток)

**Что возвращает:**
- Аудио-файл из Telegram (ogg/mp4)
- Content-Type: audio/ogg или video/mp4
- Content-Disposition: inline

**Используемые поля:**
- `Submission.answerFileId` - Telegram file_id

**Файлы:**
- `backend/src/submissions/audio-submissions.service.ts` → `getAudioFile()`
- `backend/src/submissions/audio-submissions.controller.ts` → `GET /audio-submissions/play/:fileId`

---

## 📋 Структура данных в БД

### Submission (модель)

**Поля, используемые в отчётах:**
- ✅ `answerText` - текстовый ответ (или транскрипт аудио)
- ✅ `answerFileId` - file_id из Telegram (для аудио/видео/файлов)
- ✅ `answerType` - TEXT, AUDIO, VIDEO, FILE
- ✅ `aiScore` - оценка от ИИ (0-maxScore)
- ✅ `aiFeedback` - комментарий от ИИ
- ✅ `curatorScore` - финальная оценка куратора
- ✅ `curatorFeedback` - комментарий куратора
- ✅ `status` - SENT, AI_REVIEWED, CURATOR_APPROVED, CURATOR_RETURNED
- ✅ `resubmissionRequested` - запрошена ли повторная отправка
- ✅ `resubmissionRequestedAt` - когда запрошена
- ✅ `createdAt` - дата создания
- ✅ `updatedAt` - дата обновления
- ✅ `telegramPromptMessageId` - ID сообщения бота с инструкцией

**Поля, НЕ используемые в отчётах (но есть в БД):**
- ⚠️ `telegramPromptMessageId` - не используется в отчётах

---

### Enrollment (модель)

**Поля, используемые в отчётах:**
- ✅ `status` - LOCKED, IN_PROGRESS, COMPLETED
- ✅ `unlockedAt` - когда модуль был открыт
- ✅ `completedAt` - когда модуль был завершён
- ✅ `unlockedById` - ID куратора, открывшего модуль

**Поля, НЕ используемые в отчётах (но есть в БД):**
- ⚠️ `unlockedById` - используется только в детальном прогрессе участника, не в сводных отчётах

**Поля, ОТСУТСТВУЮЩИЕ в БД:**
- ❌ `createdAt` - нет поля для отслеживания, когда Enrollment был создан
- ❌ `updatedAt` - нет поля для отслеживания изменений

---

### User (модель)

**Поля, используемые в отчётах:**
- ✅ `id`, `telegramId`, `firstName`, `lastName`
- ✅ `position` - используется для разреза по должностям
- ✅ `role` - LEARNER, CURATOR, ADMIN
- ✅ `createdAt` - дата регистрации

**Поля, НЕ используемые в отчётах (но есть в БД):**
- ⚠️ `profileCompleted` - не используется в отчётах
- ⚠️ `updatedAt` - не используется в отчётах

---

### Course / CourseModule / CourseStep (модели)

**Поля, используемые в отчётах:**
- ✅ `Course`: id, title, description, createdAt, updatedAt
- ✅ `CourseModule`: id, index, title, description, isExam, createdAt, updatedAt
- ✅ `CourseStep`: id, index, title, type, content, isRequired, maxScore, expectedAnswer, requiresAiReview, aiRubric

**Поля, НЕ используемые в отчётах (но есть в БД):**
- ⚠️ `CourseModule.autoUnlockForNewLearners` - не используется в отчётах
- ⚠️ `CourseStep.formSchema` - JSON-схема формы, не используется в отчётах

---

## 🔍 Логика работы с данными

### Текстовые ответы

**Хранение:**
- `Submission.answerText` - хранится как строка в БД
- Используется для TEXT-ответов
- Используется для транскриптов AUDIO-ответов (после распознавания через Whisper)

**Обработка:**
- Для TEXT: сохраняется напрямую
- Для AUDIO: сначала транскрибируется через `AiService.transcribeAudio()`, затем сохраняется в `answerText`

**Файлы:**
- `backend/src/submissions/submissions.service.ts` → `create()` - создание TEXT-сдачи
- `backend/src/submissions/audio-submissions.service.ts` → `processAudioSubmission()` - обработка AUDIO с транскрипцией

---

### Аудио/видео ответы

**Хранение:**
- `Submission.answerFileId` - Telegram file_id (строка)
- `Submission.answerType` - AUDIO, VIDEO, FILE
- Файлы НЕ хранятся локально, только file_id для доступа через Telegram API

**Обработка:**
- При создании AUDIO-сдачи: файл скачивается из Telegram, транскрибируется через Whisper, транскрипт сохраняется в `answerText`
- Для воспроизведения: файл скачивается из Telegram по file_id через `AudioSubmissionsService.getAudioFile()`

**Файлы:**
- `backend/src/submissions/audio-submissions.service.ts` → `processAudioSubmission()`, `getAudioFile()`

---

### Оценки и статусы

**aiScore / curatorScore:**
- `aiScore` - Float (0-maxScore), заполняется после проверки ИИ
- `curatorScore` - Float (0-maxScore), заполняется куратором при одобрении
- Оба поля могут быть null

**status:**
- `SENT` - отправлено, ожидает проверки
- `AI_REVIEWED` - проверено ИИ, ожидает проверки куратором
- `CURATOR_APPROVED` - одобрено куратором
- `CURATOR_RETURNED` - возвращено на доработку

**Обработка:**
- При создании: статус = `SENT`
- Если `requiresAiReview = true`: автоматически запускается `reviewWithAI()`, статус меняется на `AI_REVIEWED`
- Куратор может одобрить (`CURATOR_APPROVED`) или вернуть (`CURATOR_RETURNED`)

**Файлы:**
- `backend/src/submissions/submissions.service.ts` → `reviewWithAI()`, `updateStatus()`
- `backend/src/submissions/admin.controller.ts` → `POST /admin/submissions/:id/approve`, `POST /admin/submissions/:id/return`

---

## ❌ Что НЕ реализовано

### CSV/Excel экспорт

**Статус:** ❌ Не реализовано

**Что могло бы быть:**
- Экспорт списка участников в CSV
- Экспорт сдач с оценками в Excel
- Экспорт статистики по модулям в CSV

**Библиотеки:** В `package.json` нет библиотек для CSV/Excel (например, `csv-writer`, `xlsx`, `exceljs`)

---

### Экспорт сырых данных

**Статус:** ❌ Не реализовано

**Что могло бы быть:**
- Экспорт всех Submission с полными данными (answerText, aiFeedback, curatorFeedback)
- Экспорт всех Enrollment с историей
- Экспорт всех User с полной информацией

---

### Экспорт транскриптов аудио

**Статус:** ⚠️ Частично реализовано

**Текущее состояние:**
- Транскрипты сохраняются в `Submission.answerText` для AUDIO-ответов
- Доступны через JSON API (`GET /admin/submissions/:id`)
- НЕ включены в HTML-отчёт по курсу

**Что могло бы быть:**
- Отдельный экспорт всех транскриптов
- Включение транскриптов в сводные отчёты

---

### Экспорт комментариев (aiFeedback, curatorFeedback)

**Статус:** ⚠️ Частично реализовано

**Текущее состояние:**
- Комментарии доступны через JSON API
- НЕ включены в HTML-отчёт по курсу

**Что могло бы быть:**
- Включение комментариев в отчёты
- Экспорт всех комментариев отдельным файлом

---

## 📦 Потенциально важные поля для аналитики

### Поля, которые есть в БД, но не используются в отчётах:

1. **Submission.telegramPromptMessageId**
   - ID сообщения бота с инструкцией
   - Может быть полезно для аналитики взаимодействия с ботом

2. **Enrollment.unlockedById**
   - ID куратора, открывшего модуль
   - Может быть полезно для аналитики работы кураторов

3. **User.profileCompleted**
   - Завершена ли регистрация
   - Может быть полезно для аналитики конверсии регистрации

4. **CourseModule.autoUnlockForNewLearners**
   - Автоматическое открытие для новых учеников
   - Может быть полезно для аналитики политики открытия модулей

5. **CourseStep.formSchema**
   - JSON-схема динамической формы
   - Может быть полезно для аналитики типов заданий

---

### Поля, которых НЕТ в БД, но могли бы быть полезны:

1. **Enrollment.createdAt**
   - Когда Enrollment был создан
   - Полезно для аналитики времени начала обучения

2. **Submission.reviewedAt**
   - Когда куратор проверил сдачу
   - Полезно для точной аналитики SLA

3. **Submission.reviewedById**
   - Какой куратор проверил сдачу
   - Полезно для аналитики работы кураторов

4. **Submission.aiReviewDuration**
   - Время проверки ИИ (в секундах)
   - Полезно для аналитики производительности ИИ

5. **Submission.audioTranscriptionDuration**
   - Время транскрипции аудио (в секундах)
   - Полезно для аналитики производительности транскрипции

---

## 🔄 Что можно переиспользовать

### 1. CourseReportService

**Файл:** `backend/src/course/course-report.service.ts`

**Методы:**
- `buildCourseReport(courseId)` - построение полного отчёта
- `buildCourseStats()` - общие KPI
- `buildModulesReport()` - статистика по модулям
- `buildPositionsReport()` - разрез по должностям
- `buildAiVsCuratorStats()` - сравнение оценок
- `buildSlaStats()` - SLA метрики
- `buildProblemsReport()` - проблемные зоны

**Можно переиспользовать для:**
- CSV/Excel экспорта тех же данных
- JSON API для получения структурированных данных
- Других форматов отчётов

---

### 2. UsersService.getLearnersWithProgress()

**Файл:** `backend/src/users/users.service.ts`

**Метод:** `getLearnersWithProgress()`

**Что делает:**
- Загружает всех LEARNER пользователей
- Агрегирует enrollments и submissions
- Возвращает структурированные данные

**Можно переиспользовать для:**
- CSV экспорта списка участников
- Excel экспорта с форматированием

---

### 3. SubmissionsService.findAllWithFilters()

**Файл:** `backend/src/submissions/submissions.service.ts`

**Метод:** `findAllWithFilters(moduleId?, status?)`

**Что делает:**
- Загружает все сдачи с фильтрами
- Включает связанные данные (step, module, user)

**Можно переиспользовать для:**
- CSV/Excel экспорта всех сдач
- Экспорта сдач по модулю
- Экспорта сдач по статусу

---

### 4. TelegramService.sendDocument()

**Файл:** `backend/src/telegram/telegram.service.ts`

**Метод:** `sendDocument(telegramId, htmlContent, filename, caption?)`

**Что делает:**
- Отправляет HTML-файл через Telegram Bot API

**Можно переиспользовать для:**
- Отправки CSV/Excel файлов
- Отправки других форматов отчётов

---

## 📝 Резюме

### Что уже есть:

1. ✅ **HTML-отчёт по курсу** - полноценный отчёт с метриками, таблицами, проблемными зонами
2. ✅ **JSON API** - структурированные данные по участникам, сдачам, модулям
3. ✅ **Бинарные файлы** - воспроизведение аудио из Telegram
4. ✅ **Отправка через Telegram** - HTML-файлы отправляются кураторам

### Что отсутствует:

1. ❌ **CSV/Excel экспорт** - нет библиотек и реализации
2. ❌ **Экспорт сырых данных** - нет эндпоинтов для bulk export
3. ⚠️ **Транскрипты в отчётах** - есть в БД, но не включены в HTML-отчёт
4. ⚠️ **Комментарии в отчётах** - есть в БД, но не включены в HTML-отчёт

### Что можно переиспользовать:

1. ✅ **CourseReportService** - вся логика построения отчёта
2. ✅ **UsersService.getLearnersWithProgress()** - данные по участникам
3. ✅ **SubmissionsService.findAllWithFilters()** - данные по сдачам
4. ✅ **TelegramService.sendDocument()** - отправка файлов

### Важные поля для будущих экспортов:

- `Submission.answerText` - текстовые ответы и транскрипты ✅
- `Submission.aiFeedback` - комментарии ИИ ⚠️ (есть в БД, не в отчётах)
- `Submission.curatorFeedback` - комментарии куратора ⚠️ (есть в БД, не в отчётах)
- `Enrollment.unlockedById` - кто открыл модуль ⚠️ (есть в БД, не в сводных отчётах)
- `Submission.telegramPromptMessageId` - ID сообщения бота ⚠️ (есть в БД, не используется)

---

**Вывод:** В проекте уже есть хорошая основа для экспорта данных (HTML-отчёты, JSON API), но отсутствуют CSV/Excel экспорты. Большая часть данных доступна через существующие сервисы и может быть переиспользована для новых форматов экспорта.

