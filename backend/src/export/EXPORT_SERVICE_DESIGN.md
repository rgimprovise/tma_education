# Проектирование системы экспорта данных для анализа в ИИ

**Дата создания:** 2025-11-30  
**Статус:** Проектирование завершено, готово к реализации

---

## 🎯 Цель

Создать единую систему экспорта "сырых" данных для последующего анализа в ИИ:
- Поиск слов-паразитов
- Анализ ошибок
- Оценка сложности формулировок
- Анализ эффективности обучения

---

## 📋 Типы экспорта

### 1. SUBMISSIONS (Сырые данные по сдачам)

**Назначение:** Экспорт всех ответов участников с полными данными для анализа текстового контента.

**Структура:** `SubmissionExportRow[]`

**Ключевые поля для анализа:**
- `answerTextOrTranscript` - **главное поле** для анализа (текст ответа или транскрипт аудио)
- `aiFeedback` - комментарии ИИ (для анализа качества проверки)
- `curatorFeedback` - комментарии куратора (для анализа обратной связи)
- `aiScore`, `curatorScore` - оценки для анализа корреляции
- `status` - статус для фильтрации по этапам проверки

**Использование:**
- Анализ текстовых ответов на наличие слов-паразитов
- Поиск типичных ошибок в формулировках
- Оценка сложности языка
- Анализ качества обратной связи (aiFeedback, curatorFeedback)

---

### 2. USER_PROGRESS (Агрегированный прогресс)

**Назначение:** Экспорт сводной статистики по каждому участнику для анализа общего прогресса.

**Структура:** `UserProgressExportRow[]`

**Ключевые поля для анализа:**
- `completionPercent` - процент завершения курса
- `avgAiScore`, `avgCuratorScore` - средние оценки
- `returnsCount`, `returnsPercent` - количество и процент возвратов
- `activityPeriodDays` - период активности

**Использование:**
- Выявление проблемных участников (низкий completionPercent, высокий returnsPercent)
- Анализ эффективности обучения
- Сравнение прогресса между участниками
- Анализ корреляции между оценками ИИ и куратора

---

## 🔧 Параметры экспорта

### Обязательные:
- `courseId` (string) - ID курса

### Опциональные:
- `moduleId` (string) - фильтр по модулю
- `dateFrom` (ISO 8601) - начальная дата (фильтр по createdAt)
- `dateTo` (ISO 8601) - конечная дата (фильтр по createdAt)

### Выбор типа и формата:
- `type` (enum): `SUBMISSIONS` | `USER_PROGRESS`
- `format` (enum): `CSV` | `TSV` | `JSON`

---

## 📊 Форматы экспорта

### CSV (Comma-Separated Values)
- **Разделитель:** запятая
- **Кодировка:** UTF-8 с BOM (для корректного отображения в Excel)
- **Экранирование:** значения с запятыми/кавычками оборачиваются в кавычки
- **Использование:** Excel, Google Sheets, большинство инструментов анализа

### TSV (Tab-Separated Values)
- **Разделитель:** табуляция
- **Кодировка:** UTF-8
- **Обработка:** табуляции и переносы строк заменяются на пробелы
- **Использование:** текстовые редакторы, некоторые ИИ-инструменты, обработка в командной строке

### JSON (JavaScript Object Notation)
- **Формат:** массив объектов
- **Кодировка:** UTF-8
- **Форматирование:** с отступами (pretty-print)
- **Использование:** программная обработка, некоторые ИИ-инструменты, интеграции

---

## 🔐 Авторизация и доступ

### Роли с доступом:
- **ADMIN** - полный доступ ко всем экспортам
- **CURATOR** - доступ к экспортам (под контролем, может быть ограничен в будущем)

### Ограничения:
- Экспорт только по курсам, к которым у пользователя есть доступ
- Фильтрация по датам для ограничения объёма данных
- В будущем: лимиты на размер экспорта, rate limiting

---

## 📁 Структура файлов

```
backend/src/export/
├── export.service.ts                    # Основной сервис экспорта
├── export.module.ts                     # NestJS модуль (будет создан)
├── export.controller.ts                 # Контроллер (будет создан)
├── dto/
│   ├── export-params.dto.ts             # Параметры запроса
│   ├── submission-export-row.dto.ts     # Интерфейс строки экспорта сдач
│   └── user-progress-export-row.dto.ts  # Интерфейс строки экспорта прогресса
└── EXPORT_SERVICE_DESIGN.md            # Этот файл
```

---

## 🔄 Логика работы ExportService

### 1. Валидация параметров
- Проверка существования курса
- Проверка существования модуля (если указан)
- Проверка принадлежности модуля курсу
- Парсинг дат

### 2. Загрузка данных
- **Для SUBMISSIONS:** загрузка всех Submission с полными данными (user, module, step, course)
- **Для USER_PROGRESS:** загрузка всех Enrollment и Submission, группировка по пользователям

### 3. Преобразование данных
- Маппинг Prisma моделей в интерфейсы экспорта
- Формирование `answerTextOrTranscript` (текст или транскрипт)
- Форматирование имён пользователей
- Вычисление агрегированных метрик (для USER_PROGRESS)

### 4. Форматирование
- Преобразование в выбранный формат (CSV/TSV/JSON)
- Экранирование специальных символов
- Добавление BOM для CSV (для Excel)

---

## 🚀 Планируемый API

### Эндпоинт (будет создан на следующем шаге):

```
GET /admin/export/data
```

**Query параметры:**
- `courseId` (required)
- `moduleId` (optional)
- `dateFrom` (optional, ISO 8601)
- `dateTo` (optional, ISO 8601)
- `type` (required, 'submissions' | 'user-progress')
- `format` (required, 'csv' | 'tsv' | 'json')

**Response:**
- Content-Type зависит от формата:
  - CSV: `text/csv; charset=utf-8`
  - TSV: `text/tab-separated-values; charset=utf-8`
  - JSON: `application/json; charset=utf-8`
- Content-Disposition: `attachment; filename="export_<type>_<date>.csv"`

**Пример запроса:**
```
GET /admin/export/data?courseId=xxx&type=submissions&format=csv&dateFrom=2025-01-01
```

---

## 🔮 Потенциальные расширения (будущее)

### Поля для SubmissionExportRow:

1. **errorTags: string[]**
   - Категории ошибок (например: ["орфография", "пунктуация", "стиль"])
   - Может быть добавлено через отдельную таблицу `SubmissionErrorTags` или JSON поле
   - Полезно для категоризации проблем в ответах

2. **speechIssuesTags: string[]**
   - Теги речевых проблем (например: ["слова-паразиты", "паузы", "повторы"])
   - Может быть добавлено через отдельную таблицу или JSON поле
   - Полезно для анализа аудио-ответов и транскриптов

3. **complexityScore: number**
   - Сложность формулировок (оценка от 1 до 10)
   - Может быть вычислена ИИ или добавлена куратором
   - Полезно для анализа уровня сложности ответов

4. **answerLength: number**
   - Длина ответа в символах
   - Может быть вычислена автоматически
   - Полезно для анализа краткости/развёрнутости ответов

5. **answerWordCount: number**
   - Длина ответа в словах
   - Может быть вычислена автоматически
   - Полезно для анализа объёма ответов

6. **aiReviewDuration: number**
   - Время проверки ИИ (в секундах)
   - Может быть добавлено в будущем для аналитики производительности

7. **transcriptionDuration: number**
   - Время транскрипции аудио (в секундах)
   - Может быть добавлено в будущем для аналитики производительности

### Поля для UserProgressExportRow:

1. **avgAnswerLength: number**
   - Средняя длина ответов (в символах)
   - Может быть вычислена из answerTextOrTranscript всех сдач

2. **avgAnswerWordCount: number**
   - Средняя длина ответов (в словах)
   - Может быть вычислена из answerTextOrTranscript всех сдач

3. **fillerWordsCount: number**
   - Количество уникальных слов-паразитов
   - Может быть вычислено ИИ при анализе ответов

4. **avgComplexityScore: number**
   - Средняя сложность формулировок (оценка от 1 до 10)
   - Может быть вычислена ИИ при анализе ответов

5. **commonErrorCategories: string[]**
   - Категории наиболее частых ошибок
   - Может быть добавлено через анализ всех сдач пользователя

6. **progressTrend: 'improving' | 'declining' | 'stable'**
   - Тренд прогресса (улучшение/ухудшение/стабильно)
   - Может быть вычислен на основе изменения оценок со временем

---

## 📝 Особенности реализации

### Обработка answerTextOrTranscript:

1. **Для TEXT:**
   - Используется `Submission.answerText` напрямую

2. **Для AUDIO/VIDEO:**
   - Используется `Submission.answerText` (транскрипт после распознавания через Whisper)
   - Если транскрипта нет: `"[AUDIO_FILE]"` или `"[VIDEO_FILE]"`

3. **Для FILE:**
   - Всегда `"[FILE]"` (текстового контента нет)

### Агрегация для USER_PROGRESS:

1. **modulesCount:**
   - Подсчитываются только модули с обязательными шагами (`isRequired = true`)

2. **completionPercent:**
   - Вычисляется: `(completedModulesCount / modulesCount) * 100`
   - Если `modulesCount = 0`, то `0`

3. **avgAiScore / avgCuratorScore:**
   - Вычисляется только по сдачам, где оценка не null
   - Если нет оценок, то `null`

4. **activityPeriodDays:**
   - Вычисляется только если есть минимум 2 сдачи
   - Если только одна сдача или нет сдач, то `null`

---

## 🔍 Оптимизация запросов

### Для SUBMISSIONS:
- Один запрос с `include` для всех связанных данных
- Сортировка: по дате создания, затем по пользователю, модулю, шагу
- Избегание N+1 через правильные `include`

### Для USER_PROGRESS:
- Загрузка курса с модулями и enrollments одним запросом
- Загрузка всех submissions одним запросом
- Группировка и агрегация в памяти (для небольших объёмов данных)

### Для больших объёмов (будущее):
- Пагинация экспорта
- Стриминг данных
- Фоновые задачи для генерации больших экспортов

---

## ✅ Готово к реализации

1. ✅ Интерфейсы данных (`SubmissionExportRow`, `UserProgressExportRow`)
2. ✅ Параметры экспорта (`ExportParamsDto`)
3. ✅ Логика сервиса (`ExportService`)
4. ✅ Описание форматов (CSV, TSV, JSON)
5. ✅ Описание потенциальных расширений

---

## ⏳ Следующие шаги

1. Создать `ExportModule` и зарегистрировать в `AppModule`
2. Создать `ExportController` с эндпоинтом `GET /admin/export/data`
3. Добавить валидацию параметров через DTO
4. Протестировать на реальных данных
5. Добавить обработку ошибок и логирование

---

**Примечание:** На данном этапе эндпоинты НЕ создаются, только проектирование завершено.

