# Развёртывание: Шаг 5 - Полный дашборд курса

## Обзор изменений

Расширен `CourseDashboardPage` до полноценного дашборда курса с подробной статистикой и улучшенными карточками модулей.

---

## Изменения в TMA

### 1. Блок статистики по курсу

**Расположение:** После header курса, перед списком модулей

**4 карточки статистики:**

**Карточка 1: Модули**
- Иконка: 📖
- Значение: количество модулей в курсе
- Подпись: "Модуль / Модуля / Модулей"

**Карточка 2: Шаги**
- Иконка: 📝
- Значение: общее количество шагов во всех модулях
- Подпись: "Шаг / Шага / Шагов"

**Карточка 3: Участники**
- Иконка: 👥
- Значение: уникальные участники курса
- Подпись: "Участник / Участника / Участников"
- Подсказка: "Зарегистрировано в курсе"

**Карточка 4: Открытые модули**
- Иконка: 🔓
- Значение: количество модулей с enrollments
- Подпись: "Модуль открыт / Модуля открыто / Модулей открыто"
- Подсказка: "🔒 N закрыто" (если есть закрытые)

**Подсчёт статистики:**
```typescript
const totalModules = course.modules.length;
const totalSteps = course.modules.reduce((sum, m) => sum + m.stepsCount, 0);
const uniqueLearners = Math.max(...course.modules.map(m => m.enrollmentsCount), 0);
const modulesWithEnrollments = course.modules.filter(m => m.enrollmentsCount > 0).length;
const lockedModules = totalModules - modulesWithEnrollments;
```

---

### 2. Улучшенные карточки модулей

**Новая структура карточки:**

```tsx
<div className="module-card">
  <div className="module-card-content">
    {/* Header */}
    <div className="module-card-header">
      <div className="module-title-wrapper">
        <h3 className="module-card-title">
          📖 Модуль 1: Введение в пирамиду Минто
        </h3>
        <div className="module-badges">
          <span className="module-card-index">Модуль 1</span>
          <span className="module-badge exam-badge">Экзамен</span>
        </div>
      </div>
    </div>

    {/* Description */}
    <p className="module-card-description">...</p>

    {/* Stats */}
    <div className="module-card-stats">
      <span>📝 6 шагов</span>
      <span>👥 5 участников</span>
    </div>

    {/* Status */}
    <div className="module-status">
      <div className="status-badge status-unlocked">
        🔓 Открыт для 5 учеников
      </div>
      {/* ИЛИ */}
      <div className="status-badge status-locked">
        🔒 Ни для кого не открыт
      </div>
    </div>
  </div>

  {/* Actions */}
  <div className="module-card-actions">
    <button className="btn-unlock">🔓 Открыть для всех</button>
    <button className="btn-edit">✏️ Редактировать</button>
  </div>
</div>
```

**Визуальные индикаторы:**
- 🔓 Зелёный бейдж — модуль открыт
- 🔒 Красный бейдж — модуль закрыт
- Количество участников на карточке

---

### 3. Кнопки действий

**Кнопка "Открыть для всех":**
- Цвет: Primary (синий/индиго)
- Функция: `POST /admin/modules/:id/unlock { forAll: true }`
- Состояния:
  - По умолчанию: "🔓 Открыть для всех"
  - В процессе: "🔄 Открываю..." (disabled)
  - После успеха: обновляется счётчик участников

**Кнопка "Редактировать":**
- Цвет: Secondary (серый border)
- Функция: переход к `/curator/course/modules/:moduleId`
- Hover: soft primary background

---

### 4. Навигация

**Клик по карточке модуля (content area):**
- Переход к списку шагов: `/curator/course/modules/:moduleId/steps`
- (В будущем можно сделать дашборд модуля)

**Кнопка "Редактировать":**
- Переход к редактору: `/curator/course/modules/:moduleId`

---

## Стили

### Новые классы:

**Статистика:**
```css
.stats-section { margin-bottom: 32px; }
.stats-grid { grid: auto-fit, minmax(200px, 1fr); gap: 16px; }
.stat-card { flex: icon + content, hover effect }
.stat-icon-large { font-size: 40px; }
.stat-value { font-size: 32px, bold; }
.stat-label { uppercase, 14px, secondary; }
.stat-hint { 12px, secondary; }
```

**Модули:**
```css
.module-title-wrapper { flex-direction: column; }
.module-badges { flex, gap: 8px; }
.module-status { margin-top: 12px; }
.status-badge { inline-flex, padding, border-radius; }
.status-unlocked { green bg; }
.status-locked { red bg; }
```

**Кнопки:**
```css
.btn-edit { 
  border: 2px solid border;
  background: surface;
  color: text-primary;
  hover: primary-soft bg, primary border;
}
```

---

## Развёртывание на VPS

### Автоматический способ:

```bash
ssh root@79.132.140.13
cd /var/www/tma_education
bash DEPLOY_STEP5.sh
```

### Ручной способ:

```bash
ssh root@79.132.140.13
cd /var/www/tma_education

# 1. Получить обновления
git pull

# 2. Пересобрать TMA (только TMA изменилась)
cd tma
npm run build

# 3. Проверить
curl -I https://tma.n8nrgimprovise.space
```

**Примечание:** Backend не требует пересборки, так как изменения только в TMA.

---

## Проверка работы

### В браузере:

1. Откройте TMA как куратор/администратор
2. Перейдите: **Вкладка "Курсы"** → **"Пирамида Минто"**
3. Вы должны увидеть:

**Блок статистики:**
- 📖 4 Модуля
- 📝 23 Шага (или сколько есть)
- 👥 X Участников
- 🔓 Y Модулей открыто (🔒 Z закрыто)

**Карточки модулей:**
- Модуль 1: Введение в пирамиду Минто
  - Статус: 🔓 Открыт для 5 учеников (или 🔒 Ни для кого не открыт)
  - Кнопки: "🔓 Открыть для всех" | "✏️ Редактировать"
- Модуль 2: ...
- Модуль 3: ...
- Модуль 4: Итоговая аттестация (🎓 Экзамен)

**Действия:**
- Нажмите "🔓 Открыть для всех" на Модуле 1
- Подтвердите действие
- Статус обновится: "🔓 Открыт для N учеников"
- Счётчик в статистике обновится

---

## Визуальный дизайн

### Цветовая схема статусов:

**Открыт (Unlocked):**
- Фон: #d4edda (светло-зелёный)
- Текст: #155724 (тёмно-зелёный)
- Иконка: 🔓

**Закрыт (Locked):**
- Фон: #f8d7da (светло-красный)
- Текст: #721c24 (тёмно-красный)
- Иконка: 🔒

### Кнопки:

**"Открыть для всех":**
- Border: Primary
- Background: Surface (белый)
- Hover: Primary background, white text

**"Редактировать":**
- Border: Border (светло-серый)
- Background: Surface
- Hover: Primary-soft background, primary border

---

## Будущие улучшения

### 1. Детальная статистика участников

**Вместо approximation (max enrollments):**
```typescript
// Получить уникальных участников через SQL или API
const uniqueLearnersResponse = await api.get(`/admin/courses/${courseId}/learners`);
const uniqueLearners = uniqueLearnersResponse.data.length;
```

**Backend endpoint:**
```typescript
// GET /admin/courses/:id/learners
// Возвращает список уникальных userId с enrollments по модулям курса
```

---

### 2. Статистика прогресса участников

**Добавить карточки:**
- "В процессе" (learners с хотя бы одним IN_PROGRESS enrollment)
- "Завершили курс" (learners со всеми модулями COMPLETED)
- "Средний прогресс" (% от всех шагов)

---

### 3. Блок "Нуждаются в проверке"

**Отображать submissions:**
- Статус: SENT или AI_REVIEWED
- По всем модулям курса
- Группировка по модулям
- Быстрый переход к проверке

---

### 4. Блок "Отстающие ученики"

**Критерии:**
- Не открыли модуль больше N дней
- Не сдали ни одного задания
- Застряли на одном шаге

**Действия:**
- Отправить напоминание
- Открыть дополнительный модуль
- Связаться с учеником

---

## Troubleshooting

### Статистика показывает 0 участников

**Причина:** Нет enrollments или данные не загрузились

**Решение:**
1. Проверьте enrollments:
   ```bash
   sudo -u postgres psql -d minto_db -c \
     "SELECT COUNT(*) FROM \"Enrollment\";"
   ```
2. Откройте модуль через кнопку "Открыть для всех"

---

### Статус модуля не обновляется после unlock

**Причина:** Данные не перезагрузились

**Решение:**
- После unlock вызывается `await loadCourseData()`
- Если не помогло → перезагрузите страницу TMA вручную
- Проверьте console.log в браузере

---

### Кнопка "Редактировать" не работает

**Причина:** Роль пользователя не ADMIN

**Решение:**
- Редактор модулей доступен только для ADMIN
- Проверьте роль: `GET /users/me`

---

## Откат изменений

```bash
cd /var/www/tma_education
git log --oneline -5
git checkout <previous-commit-hash>

cd tma
npm run build
```

---

**Версия:** 5.0  
**Дата:** 2025-11-29  
**Изменения:** Полноценный дашборд курса с статистикой и управлением модулями

