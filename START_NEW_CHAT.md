# 🚀 ИНСТРУКЦИЯ ДЛЯ НОВОГО ЧАТА

## 📋 Что сделать перед началом работы в новом чате:

### 1. Открой новый чат в Cursor

### 2. Отправь AI следующее сообщение:

```
Привет! Я работаю над проектом системы обучения «Пирамида Минто».

Пожалуйста, внимательно прочитай следующие файлы перед началом работы:

1. @NEW_CHAT_CONTEXT.md - полный контекст проекта
2. @PROJECT_RULES.md - строгие правила разработки (ОБЯЗАТЕЛЬНО!)

После прочтения подтверди, что ты понял:
- Архитектуру проекта (NestJS backend + React TMA)
- Правила разработки (всегда проверять npm run build, указывать все шаги деплоя)
- Типичные ошибки и как их избежать
- Структуру БД и Prisma workflow

Начни работу только после подтверждения!
```

### 3. AI должен подтвердить понимание

После этого можно начинать работу над задачами.

---

## ⚠️ ВАЖНО

Если AI:
- ❌ Забывает указать пересборку TMA → напомни про `PROJECT_RULES.md`
- ❌ Пишет код, который не компилируется → напомни про чеклист перед коммитом
- ❌ Не указывает `npx prisma generate` → напомни про правила работы с Prisma

**Все правила описаны в `PROJECT_RULES.md` - ссылайся на него при необходимости!**

---

## 📝 Альтернативный вариант (если AI не читает файлы)

Скопируй и вставь в новый чат:

```
Ты — старший разработчик, работающий над проектом системы обучения «Пирамида Минто».

ПРОЕКТ:
- Backend: NestJS + Prisma + PostgreSQL + Telegram Bot (grammY) + OpenAI
- Frontend: React + Vite (Telegram Mini App)
- Deployment: VPS с PM2, Caddy

КРИТИЧЕСКИ ВАЖНЫЕ ПРАВИЛА:

1. ВСЕГДА проверяй npm run build перед коммитом (и backend, и TMA)
2. После изменения schema.prisma → npx prisma generate → npm run build
3. При деплое указывай ВСЕ шаги: backend + TMA (если изменялись оба)
4. НЕ используй any без крайней необходимости
5. Проверяй линтер перед коммитом

ТИПИЧНЫЕ ОШИБКИ, КОТОРЫЕ НУЖНО ИЗБЕГАТЬ:
- Забыл пересобрать TMA → ВСЕГДА указывать в инструкции деплоя
- Забыл npx prisma generate → ВСЕГДА после изменения schema.prisma
- Неправильные типы TypeScript → ВСЕГДА проверять npm run build локально
- Неправильные импорты → Проверять все импорты
- Circular dependencies → Использовать ModuleRef для lazy loading

ЧЕКЛИСТ ПЕРЕД КОММИТОМ:
- npm run lint (backend и/или TMA)
- npm run build (backend и/или TMA)
- Если изменялся schema.prisma: npx prisma generate → npm run build

ШАБЛОН ИНСТРУКЦИИ ДЕПЛОЯ:
Всегда указывай оба раздела (backend + TMA), даже если изменялся только один!

Backend:
cd /var/www/tma_education/backend
git pull
# ⚠️ Если изменялся schema.prisma:
npx prisma generate
npx prisma migrate deploy
npm run build
pm2 restart minto-backend

TMA:
cd /var/www/tma_education/tma
git pull
npm run build

Подтверди, что понял все правила, и начни работу!
```

---

## ✅ После этого AI будет:

- ✅ Всегда проверять `npm run build` перед коммитом
- ✅ Всегда указывать пересборку TMA при изменениях
- ✅ Всегда указывать `npx prisma generate` при изменении схемы
- ✅ Использовать правильные типы TypeScript
- ✅ Следовать всем правилам из `PROJECT_RULES.md`

---

**Удачи! 🚀**

