# Обновление TMA: Learner UX и проверка регистрации

## Изменения в интерфейсе для обучающихся

### 1. Проверка завершения регистрации

**Проблема:** Пользователи могли входить в TMA без завершённой регистрации через бота.

**Решение:**
- Добавлена проверка `profileCompleted` при авторизации
- Если профиль не завершён → редирект на `/incomplete-profile`
- Показывается подсказка с инструкцией по завершению регистрации в боте

**Файлы:**
- `tma/src/pages/IncompleteProfilePage.tsx` — новая страница
- `tma/src/pages/IncompleteProfilePage.css` — стили
- `tma/src/pages/LoginPage.tsx` — добавлена проверка profileCompleted
- `tma/src/contexts/AuthContext.tsx` — добавлено поле `profileCompleted` в User

---

### 2. Заглушка для пользователей без открытых модулей

**Проблема:** Если у пользователя нет enrollments, он видел пустой список модулей без объяснения.

**Решение:**
- В `DashboardPage` добавлена проверка на наличие доступных модулей (IN_PROGRESS или COMPLETED)
- Если нет доступных модулей → показывается красивая заглушка:
  - 🔒 Иконка
  - Заголовок: "Модули пока не открыты"
  - Текст: "Пока для вас не открыт ни один модуль. Куратор откроет первый модуль, когда начнётся обучение."
  - Подсказка: "Вы получите уведомление в Telegram, когда модуль будет открыт."

**Файлы:**
- `tma/src/pages/DashboardPage.tsx` — добавлена проверка hasAccessibleModules
- `tma/src/pages/DashboardPage.css` — стили для empty-state

---

### 3. Правильная маршрутизация

**Было:**
- `/` → `/login`
- После авторизации → `/dashboard` для LEARNER

**Стало:**
- `/` → `/login` (автоавторизация через Telegram WebApp)
- После авторизации:
  - Если `profileCompleted = false` → `/incomplete-profile`
  - Если `LEARNER` с завершённым профилем → `/dashboard`
  - Если `CURATOR` или `ADMIN` → `/curator`

**Файлы:**
- `tma/src/App.tsx` — обновлена структура роутов
- `tma/src/pages/LoginPage.tsx` — добавлена логика редиректа

---

## Структура страниц для LEARNER

```
┌─────────────────────────────────────────────┐
│  / (главная)                                │
│  ↓ редирект на /login                       │
└─────────────────────────────────────────────┘
                    ↓
┌─────────────────────────────────────────────┐
│  /login (LoginPage)                         │
│  - Получает initData от Telegram WebApp    │
│  - Отправляет POST /auth/telegram-webapp   │
│  - Получает JWT + user                     │
└─────────────────────────────────────────────┘
                    ↓
         ┌──────────┴──────────┐
         │                     │
         ↓                     ↓
┌──────────────────┐  ┌──────────────────┐
│ profileCompleted │  │ profileCompleted │
│     = false      │  │     = true       │
└──────────────────┘  └──────────────────┘
         ↓                     ↓
┌──────────────────┐  ┌──────────────────┐
│ /incomplete-     │  │ /dashboard       │
│  profile         │  │ (DashboardPage)  │
└──────────────────┘  └──────────────────┘
                               ↓
                    ┌──────────┴──────────┐
                    │                     │
                    ↓                     ↓
          ┌──────────────────┐  ┌──────────────────┐
          │ Нет IN_PROGRESS/ │  │ Есть IN_PROGRESS/│
          │ COMPLETED модулей│  │ COMPLETED модули │
          └──────────────────┘  └──────────────────┘
                    ↓                     ↓
          ┌──────────────────┐  ┌──────────────────┐
          │ Empty State:     │  │ Список модулей + │
          │ "Модули пока не  │  │ Кнопка "Продол-  │
          │  открыты"        │  │  жить обучение"  │
          └──────────────────┘  └──────────────────┘
```

---

## Экраны

### 1. IncompleteProfilePage (`/incomplete-profile`)

**Когда показывается:**
- После авторизации, если `profileCompleted = false`
- Или если `firstName`, `lastName` не заполнены

**Содержание:**
- 📝 Иконка
- Заголовок: "Завершите регистрацию"
- Описание: "Регистрация проходит через чат с ботом."
- Инструкция (4 шага):
  1. Вернитесь в диалог с ботом
  2. Отправьте команду `/start`
  3. Ответьте на вопросы (имя, фамилия, должность)
  4. После завершения регистрации вернитесь сюда и обновите приложение
- Кнопка: "Вернуться в чат с ботом" (закрывает WebApp)

**Дизайн:**
- Градиентный фон (фиолетовый)
- Белая карточка по центру
- Современный, дружелюбный UI

---

### 2. DashboardPage (`/dashboard`) — Empty State

**Когда показывается:**
- У пользователя нет ни одного модуля со статусом `IN_PROGRESS` или `COMPLETED`
- То есть куратор ещё не открыл ни один модуль

**Содержание:**
- 🔒 Иконка (большая)
- Заголовок: "Модули пока не открыты"
- Описание:
  ```
  Пока для вас не открыт ни один модуль.
  Куратор откроет первый модуль, когда начнётся обучение.
  ```
- Подсказка: "Вы получите уведомление в Telegram, когда модуль будет открыт."

**Дизайн:**
- Градиентный фон (серо-голубой)
- Центрированный текст
- Подсказка в полупрозрачном блоке

---

### 3. DashboardPage (`/dashboard`) — С модулями

**Когда показывается:**
- У пользователя есть хотя бы один модуль со статусом `IN_PROGRESS` или `COMPLETED`

**Содержание:**
- Приветствие: "Добро пожаловать, {firstName}!"
- Подзаголовок: "Ваш прогресс по модулям"
- Список модулей:
  - Модуль 1: Название (🔒 Заблокирован / 📚 В процессе / ✅ Завершён)
  - Модуль 2: Название (статус)
  - ...
- Если есть IN_PROGRESS модуль → кнопка "▶️ Продолжить обучение"
- Если нет IN_PROGRESS → текст "Ждите открытия модуля куратором"

**Взаимодействие:**
- Клик на модуль со статусом `IN_PROGRESS` или `COMPLETED` → переход на `/modules/:id`
- Клик на модуль со статусом `LOCKED` → ничего не происходит (карточка затемнена)

---

## Логика проверок

### В LoginPage:

```typescript
if (!user.profileCompleted || !user.firstName || !user.lastName) {
  navigate('/incomplete-profile');
  return;
}

if (user.role === 'CURATOR' || user.role === 'ADMIN') {
  navigate('/curator');
} else {
  navigate('/dashboard');
}
```

### В DashboardPage:

```typescript
const hasAccessibleModules = modules.some(
  (m) => m.enrollment.status === 'IN_PROGRESS' || m.enrollment.status === 'COMPLETED'
);

if (!hasAccessibleModules) {
  // Показать empty state
} else {
  // Показать список модулей
}
```

---

## Взаимодействие с backend

### 1. Авторизация

**Запрос:**
```
POST /auth/telegram-webapp
Body: { "initData": "..." }
```

**Ответ:**
```json
{
  "access_token": "eyJhbGc...",
  "user": {
    "id": "uuid",
    "telegramId": "123456",
    "firstName": "Иван",
    "lastName": "Петров",
    "role": "LEARNER",
    "profileCompleted": true
  }
}
```

### 2. Получение модулей

**Запрос:**
```
GET /course/modules
Headers: { "Authorization": "Bearer ..." }
```

**Ответ:**
```json
[
  {
    "id": "uuid",
    "index": 1,
    "title": "Модуль 1: Введение",
    "description": "...",
    "enrollment": {
      "id": "uuid",
      "status": "IN_PROGRESS", // или "LOCKED", "COMPLETED"
      "unlockedAt": "2025-11-29T...",
      "completedAt": null
    }
  }
]
```

---

## Сценарии использования

### Сценарий 1: Новый пользователь без регистрации

1. Пользователь открывает TMA через кнопку в боте
2. TMA открывается на `/` → редирект на `/login`
3. `LoginPage` авторизует через Telegram WebApp
4. Backend возвращает `user.profileCompleted = false`
5. Редирект на `/incomplete-profile`
6. Пользователь видит инструкцию
7. Нажимает "Вернуться в чат с ботом" → WebApp закрывается
8. Пользователь отправляет `/start` боту
9. Проходит диалог регистрации (имя → фамилия → должность)
10. Бот обновляет `profileCompleted = true`
11. Пользователь снова открывает TMA → теперь редирект на `/dashboard`

### Сценарий 2: Пользователь с регистрацией, но без модулей

1. Пользователь открывает TMA
2. Авторизация → редирект на `/dashboard`
3. Backend возвращает список модулей, но все `status = LOCKED`
4. `DashboardPage` не находит IN_PROGRESS или COMPLETED модулей
5. Показывается empty state: "Модули пока не открыты"
6. Пользователь видит, что нужно ждать куратора
7. Куратор открывает модуль → пользователь получает уведомление в Telegram
8. Пользователь обновляет TMA → видит модуль со статусом `IN_PROGRESS`

### Сценарий 3: Пользователь с открытым модулем

1. Пользователь открывает TMA
2. Авторизация → редирект на `/dashboard`
3. Backend возвращает модули, где Модуль 1 = `IN_PROGRESS`
4. `DashboardPage` находит доступные модули
5. Показывается список модулей + кнопка "Продолжить обучение"
6. Клик на кнопку → переход на `/modules/:id` первого IN_PROGRESS модуля
7. Пользователь проходит шаги модуля

---

## Обновление на VPS

```bash
# 1. Подключение к VPS
ssh root@79.132.140.13

# 2. Перейти в проект
cd /var/www/tma_education

# 3. Получить изменения
git pull

# 4. Пересобрать TMA
cd tma
npm run build

# 5. Перезапустить Caddy (если нужно)
sudo systemctl reload caddy

# 6. Проверить
# Откройте TMA в Telegram и проверьте новое поведение
```

**Примечание:** Backend не требует изменений — `profileCompleted` уже добавлено в предыдущем обновлении.

---

## Результат

✅ Пользователи с незавершённой регистрацией видят подсказку и не могут войти в приложение  
✅ Пользователи без открытых модулей видят понятное сообщение вместо пустого списка  
✅ Правильная маршрутизация в зависимости от роли и статуса профиля  
✅ Улучшенный UX с понятными сообщениями  
✅ Визуально приятные empty states  

---

**Дата обновления:** 2025-11-29  
**Версия:** 2.0

