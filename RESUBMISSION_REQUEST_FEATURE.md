# Функционал "Запросить повторную отправку ответа"

## 📋 Описание

Добавлена возможность для учеников запросить повторную отправку ответа, если они случайно отправили незавершённый или неправильный ответ.

### Важные ограничения:
- **Ученик НЕ может сам пересоздать submission** — он может только попросить куратора разрешить повторную отправку
- Куратор видит запросы и может принять решение (вернуть на доработку или одобрить текущий ответ)
- Запрос отслеживается через два поля в модели `Submission`: `resubmissionRequested` и `resubmissionRequestedAt`

## 🔧 Технические изменения

### Backend

#### 1. Обновлена модель `Submission` в Prisma

**Файл:** `backend/prisma/schema.prisma`

Добавлены два новых поля:
```prisma
model Submission {
  // ... существующие поля
  
  resubmissionRequested   Boolean   @default(false) // Ученик запросил повторную отправку
  resubmissionRequestedAt DateTime? // Когда запрошена повторная отправка
  
  // ... остальные поля
}
```

**Миграция:** Необходимо выполнить `npx prisma migrate dev` для создания миграции.

#### 2. Новый эндпоинт API

**Файл:** `backend/src/submissions/submissions.controller.ts`

```typescript
POST /submissions/:id/request-resubmission
```

**Поведение:**
1. Проверяет, что пользователь — владелец submission
2. Проверяет статус (не разрешается для `CURATOR_APPROVED`)
3. Проверяет, что запрос ещё не был сделан ранее
4. Устанавливает флаги `resubmissionRequested = true`, `resubmissionRequestedAt = now()`
5. Отправляет Telegram-уведомление всем кураторам/админам

**Ответ при успехе:**
```json
{
  "message": "Запрос на повторную отправку отправлен куратору",
  "submission": { /* обновлённая submission */ }
}
```

**Возможные ошибки:**
- `404` — Submission not found
- `403` — You can only request resubmission for your own submissions
- `400` — Cannot request resubmission for approved submissions
- `400` — Запрос на повторную отправку уже отправлен. Дождитесь ответа куратора.

#### 3. Обновлён сервис `SubmissionsService`

**Файл:** `backend/src/submissions/submissions.service.ts`

Добавлен метод `requestResubmission`:
- Проверяет все необходимые условия
- Обновляет submission в БД
- Отправляет уведомления кураторам асинхронно (не блокирует ответ)

#### 4. Обновлены DTO

**Файлы:**
- `backend/src/submissions/dto/submission-response.dto.ts`
- `backend/src/course/dto/step-response.dto.ts`

Добавлены поля:
```typescript
resubmissionRequested: boolean;
resubmissionRequestedAt?: Date;
```

#### 5. Обновлён `CourseService`

**Файл:** `backend/src/course/course.service.ts`

Методы `getStepWithProgress` и `getModuleStepsWithProgress` теперь включают новые поля в объект `submission`.

### Frontend (TMA)

#### 1. Обновлён интерфейс `Step`

**Файл:** `tma/src/pages/StepPage.tsx`

Добавлены поля в интерфейс `Step.submission`:
```typescript
resubmissionRequested: boolean;
resubmissionRequestedAt?: string;
```

#### 2. Новая функция `handleRequestResubmission`

Обрабатывает клик по кнопке "Запросить повторную отправку":
- Вызывает `POST /submissions/:id/request-resubmission`
- При успехе перезагружает данные шага
- Показывает alert через `Telegram.WebApp.showAlert`

#### 3. Новая кнопка для учеников

**Условия отображения:**
- Текущий пользователь — `LEARNER`
- Статус submission **не** `CURATOR_APPROVED`
- `resubmissionRequested === false`

**Кнопка:**
```tsx
<button
  className="btn btn-secondary"
  onClick={handleRequestResubmission}
  disabled={requestingResubmission}
>
  {requestingResubmission ? 'Отправка запроса...' : '🔄 Запросить повторную отправку'}
</button>
```

#### 4. Бейдж "Запрос отправлен"

Если `resubmissionRequested === true`, показывается вместо кнопки:
```tsx
<div className="resubmission-badge">
  🔄 Запрос на повторную отправку отправлен куратору. Ожидайте решения.
</div>
```

#### 5. Отображение в интерфейсе куратора

**Файл:** `tma/src/pages/CuratorUserPage.tsx`

Куратор видит бейдж на карточке submission:
```tsx
{submission.resubmissionRequested && (
  <div className="resubmission-request-badge">
    🔄 Запрос на повторную отправку
  </div>
)}
```

#### 6. Новые стили

**Файл:** `tma/src/pages/StepPage.css`
```css
.resubmission-badge {
  margin-top: 12px;
  padding: 12px;
  background-color: rgba(255, 152, 0, 0.1);
  border-left: 3px solid #ff9800;
  border-radius: 4px;
  /* ... */
}
```

**Файл:** `tma/src/pages/CuratorUserPage.css`
```css
.resubmission-request-badge {
  display: inline-block;
  margin-top: 8px;
  padding: 6px 12px;
  background-color: rgba(255, 152, 0, 0.15);
  border: 1px solid #ff9800;
  border-radius: 4px;
  /* ... */
}
```

## 🚀 Развёртывание

### Автоматическое (рекомендуется)
```bash
bash DEPLOY_RESUBMISSION_FEATURE.sh
```

### Ручное

```bash
# 1. Получить обновления
git pull

# 2. Создать миграцию БД
cd backend
npx prisma migrate dev --name add_resubmission_fields
npx prisma generate

# 3. Пересобрать backend
npm run build

# 4. Перезапустить backend
pm2 restart minto-backend

# 5. Пересобрать TMA
cd ../tma
npm run build

# 6. Проверить статус
pm2 status
pm2 logs minto-backend --lines 30
```

## ✅ Проверка работы

### Как ученик (LEARNER):

1. **Откройте TMA как ученик**
2. **Перейдите к любому модулю и заданию**
3. **Отправьте ответ**
4. **После отправки проверьте:**
   - ✅ Появилась кнопка "🔄 Запросить повторную отправку" (если статус не `CURATOR_APPROVED`)
   - ✅ При клике на кнопку появляется alert "✅ Запрос отправлен куратору"
   - ✅ Кнопка заменяется на бейдж "🔄 Запрос отправлен куратору"
   - ✅ После перезагрузки страницы бейдж остаётся (данные сохранены в БД)

### Как куратор (CURATOR/ADMIN):

1. **Откройте TMA как куратор**
2. **Перейдите на страницу ученика** (через "Ученики" → выбрать ученика)
3. **Просмотрите последние сдачи:**
   - ✅ На карточке сдачи с запросом видна оранжевая плашка "🔄 Запрос на повторную отправку"
   - ✅ Куратор получил Telegram-уведомление о запросе

### Проверка уведомлений Telegram:

1. **Проверьте логи backend:**
   ```bash
   pm2 logs minto-backend --lines 50
   ```
2. **Проверьте Telegram:**
   - Куратор должен получить сообщение:
     ```
     🔄 Запрос на повторную отправку
     
     Ученик: Имя Фамилия
     Модуль 1: Название модуля
     Шаг 2: Название шага
     
     Ученик просит разрешить повторную отправку ответа.
     ```

## 🎯 Польза для пользователей

### Для учеников:
- ✅ **Возможность исправить ошибку:** Если случайно отправил неполный или неправильный ответ
- ✅ **Прозрачность:** Видит, что запрос отправлен, и ожидает решения куратора
- ✅ **Не нужно писать куратору отдельно:** Запрос автоматически отправляется через систему

### Для кураторов:
- ✅ **Мгновенное уведомление:** Telegram-сообщение с деталями запроса
- ✅ **Визуальный индикатор:** Оранжевый бейдж на карточке сдачи
- ✅ **Контроль:** Куратор решает, разрешить повторную отправку (вернуть на доработку) или нет

## 📝 Логика работы (Flow)

```
1. Ученик отправляет ответ
   ↓
2. Ученик понимает, что ответ неправильный
   ↓
3. Ученик нажимает "🔄 Запросить повторную отправку"
   ↓
4. Backend:
   - Проверяет владельца, статус
   - Устанавливает resubmissionRequested = true
   - Отправляет уведомление кураторам в Telegram
   ↓
5. Куратор:
   - Получает Telegram-уведомление
   - Видит бейдж в TMA на карточке сдачи
   - Принимает решение:
     a) Вернуть на доработку (CURATOR_RETURNED) → ученик может отправить новый ответ
     b) Одобрить текущий ответ (CURATOR_APPROVED) → завершение
```

## 🔒 Защита от злоупотреблений

1. **Один запрос на сдачу:** Нельзя отправить запрос повторно, если он уже отправлен
2. **Только для своих сдач:** Нельзя запросить повторную отправку чужой сдачи
3. **Не для одобренных:** Нельзя запросить повторную отправку для уже одобренной сдачи
4. **Логирование:** Дата и время запроса сохраняются (`resubmissionRequestedAt`)

## 📂 Изменённые файлы

### Backend:
1. `backend/prisma/schema.prisma` — добавлены поля
2. `backend/src/submissions/submissions.controller.ts` — новый эндпоинт
3. `backend/src/submissions/submissions.service.ts` — метод `requestResubmission`
4. `backend/src/submissions/dto/submission-response.dto.ts` — обновлён DTO
5. `backend/src/course/dto/step-response.dto.ts` — обновлён DTO
6. `backend/src/course/course.service.ts` — обновлены методы

### Frontend (TMA):
1. `tma/src/pages/StepPage.tsx` — кнопка и логика
2. `tma/src/pages/StepPage.css` — стили для бейджа
3. `tma/src/pages/CuratorUserPage.tsx` — отображение для куратора
4. `tma/src/pages/CuratorUserPage.css` — стили для бейджа куратора

### Документация:
1. `RESUBMISSION_REQUEST_FEATURE.md` — этот файл
2. `DEPLOY_RESUBMISSION_FEATURE.sh` — скрипт развёртывания

